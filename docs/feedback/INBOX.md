# INBOX.md — 내가 던지는 지시 (작업 큐)

> 루프는 "처리 대기" 목록을 번호가 작은 순서대로 하나씩만 처리한다 (한 바퀴 = 한 항목).
> 이 목록에 미완료 항목(`- [ ]`)이 하나도 없으면 loop.sh는 세션을 새로 열지 않고
> (토큰을 쓰지 않고) 그 자리에서 스스로 종료한다.
> 완료된 항목은 지우지 말고 `- [x]`로 바꾸고 완료 날짜를 붙인다 — 지운 기록은 다음 바퀴가
> 같은 지시를 되돌릴 위험이 있다.
> 번호는 한 번 쓰이면 재사용하지 않는다. 새 지시를 추가할 때는 지금까지 쓰인 가장 큰 번호의
> 다음 번호를 쓴다.
>
> 형식 예시 (그대로 복사해서 아래 "처리 대기"에 추가하세요):
> `- [ ] #1 여기에 내용을 적으세요`

## 처리 대기

- [x] #1 (완료 2026-09-02) Godot 4 프로젝트 뼈대를 만들고, 메인 메뉴 씬(플레이 / 설정 / 나가기 버튼)을 만든다. "설정"과 "플레이"는 아직 다음 항목들이 구현되지 않았으니, 지금은 눌러도 빈 화면(또는 "준비 중")으로 넘어가도 된다. "나가기"는 실제로 앱을 종료해야 한다.
- [x] #2 (완료 2026-09-02) "플레이"를 누르면 캐릭터 슬롯 화면으로 간다. 코어키퍼처럼 슬롯 3개를 보여주고, 빈 슬롯을 고르면 캐릭터 커스터마이징 화면으로 넘어간다 (커스터마이징 항목 자체는 다음 항목에서 만들 것이므로, 지금은 임시 화면이어도 된다).
- [x] #3 (완료 2026-09-02) 캐릭터 커스터마이징 화면을 만든다. 확정하면(커스터마이징 항목이 아직 미정이니 최소한으로: 외형 몇 가지 골라볼 수 있는 정도) 캐릭터를 슬롯에 저장하고 월드 씬으로 넘어간다 (월드는 아직 빈 필드여도 된다 — 플레이어가 그 위에 존재하기만 하면 됨).
- [x] #4 (완료 2026-09-02) 월드 씬에서 플레이어 이동(WASD)과 마우스 조준(플레이어가 마우스 방향을 바라봄) 프로토타입을 만든다. DESIGN.md의 "조작" 절을 따른다.
- [x] #5 (완료 2026-09-02) 설정 화면에 해상도 선택 옵션을 만든다. DESIGN.md의 "카메라 / 해상도" 규칙(해상도를 높여도 시야가 넓어지면 안 되고 화면 비율에만 영향을 줘야 함)을 반드시 지킨다.
- [x] #6 (완료 2026-09-02) (버그 리포트) 월드에서 플레이 중일 때 화면 왼쪽 위에 있는 "메인 메뉴로 가는 버튼"을 없앤다. 대신 ESC 키를 누르면 일시정지 메뉴가 뜨고, 거기에 "설정"과 "나가기"가 있어야 한다 (설정을 누르면 기존 설정 화면으로, 나가기를 누르면 메인 메뉴로 돌아가거나 앱을 종료 — 어느 쪽이 맞을지 애매하면 STATUS.md에 남기고 "메인 메뉴로 돌아가기"로 우선 구현).
- [x] #7 (완료 2026-09-02, 조사 결과 코드 결함 아님 — 상세는 STATUS.md 참고) 설정의 해상도 옵션을 눌러도 실제 게임 해상도가 안 바뀐다. 재현: 설정에서 해상도 값을 골라도 적용되지 않고, Godot 에디터의 "임베디드 게임 창"에 있는 화면비 옵션(예: "다른 화면 비율 유지")을 눌러도 그냥 맞춰지기만 할 뿐 사용자가 설정에서 고른 값대로 창이 바뀌지 않는다. 원인을 직접 조사해서 고친다 (에디터의 임베디드 게임 창 설정과 프로젝트의 실제 해상도/스트레치 설정이 꼬여 있는 것으로 보이지만, 정확한 원인은 확인 필요). #5에서 만든 "해상도를 바꿔도 시야는 고정" 규칙은 그대로 유지한 채 고칠 것.
- [x] #8 (완료 2026-09-02) 사냥 전투를 만든다. 좌클릭으로 기본 소총 발사, 우클릭으로 기본탄/마취탄 전환. DESIGN.md "총기 스탯"(사거리 800, 데미지 25, 초당 2발, 이동 중 탄퍼짐 증가, 연사 시 반동)을 그대로 적용한다. 아직 사슴이 없어도 되니, 총알이 사거리 안에서 나가고 연사 속도 제한이 실제로 동작하는 것까지만 확인한다 (대상 명중/데미지는 다음 항목에서).
- [x] #9 (완료 2026-09-02, 물리 충돌 검증은 예산 부족으로 미완 — STATUS.md 참고) 사슴을 만든다. 필드에 사슴을 배치하고, 평소엔 배회(랜덤 이동)하다가 플레이어가 가까이 오거나 총에 맞으면 도망간다. 최대 체력 100. 기본탄에 맞으면 데미지(25)만큼 깎이고 0이 되면 사망(사냥 성공). 체력이 10(=10%) 미만인 상태에서 마취탄에 맞으면 죽는 대신 포획 처리된다(DESIGN.md "포획" 규칙).
- [x] #10 (완료 2026-09-02) 채집과 채광을 만든다. 필드에 채집 포인트(삽으로 상호작용 → 벼 씨앗 획득)와 채광 포인트(곡괭이로 상호작용 → 철 획득)를 배치하고, 얻은 아이템이 인벤토리에 들어가게 한다. DESIGN.md대로 삽/곡괭이는 한 세트 도구로 다룬다.
- [x] #11 (완료 2026-09-02) 농사를 만든다. #10에서 얻은 벼 씨앗을 밭(정해진 구역)에 심으면 시간이 지나서 벼로 자라고, 수확할 수 있게 한다.
- [x] #12 (완료 2026-09-02) 목장을 만든다. #9에서 마취탄으로 포획한 사슴을 목장 구역에 배치해서 기를 수 있게 한다 (도망가지 않고 그 구역 안에 머무름).
- [x] #13 (완료 2026-09-02) 낮/밤·계절·날씨 시스템을 만든다. 하루 20분(낮 10분+밤 10분) 타이머, 날짜가 지나면 계절이 바뀐다(1·2·3월 봄, 4·5·6월 여름, 7·8·9월 가을, 10·11·12월 겨울). DESIGN.md의 강수 확률 공식(계절 기본값 + 마지막 비 이후 지난 날 수×1%p, 비 오면 리셋, 봄/가을/겨울 기본 1%·여름 기본 30%)으로 매일 낮/밤 강수 여부를 각각 독립적으로 판정한다. 비주얼은 최소한(화면 밝기 변화, 비 표시 정도)으로 충분하고 정교한 이펙트는 나중에 한다.
- [x] #14 (완료 2026-09-02) 멀티플레이 세션 서버를 만든다. 방 만들기(방 코드 발급)와 방 코드 입력해서 참가하기를 추가하고, Godot 내장 멀티플레이(ENetMultiplayerPeer)로 같은 네트워크 안에서 호스트와 참가자가 연결되어 같은 월드에서 서로를 보고 움직이는 것까지 만든다. 인터넷 너머 매치메이킹/릴레이 서버는 범위 밖이니, 지금은 로컬 네트워크(같은 와이파이 등) 연결까지만 만든다.
- [x] #15 (완료 2026-09-02) (버그 리포트) 사슴이 방향을 바꿀 때마다 다른 개체처럼 보인다(사용자 표현: "암수/새끼가 계속 바뀐다"). 원인 확인됨: `assets/sprites/deer/deer_north.png`(점박이 새끼 느낌), `deer_south.png`(뿔 없는 평범한 성체), `deer_east.png`(뿔 달린 수사슴)가 서로 다른 사슴처럼 그려져 있다. 코드에 성별/나이 개념은 없다 — 순수 그래픽 일관성 문제다. PixelLab로 "같은 한 마리 사슴"을 4방향(north/south/east, west는 기존처럼 east 좌우반전 유지)에서 일관된 색/무늬/뿔 유무로 다시 생성해서 교체한다.
- [x] #16 (완료 2026-09-02) (버그 리포트) 전체적으로 스프라이트 크기가 너무 크다. 지금 각 씬의 Sprite scale이 플레이어/사슴=3, 목장(ranch_zone)=4, 채집/채광 포인트=2.2, 밭(farm_plot)=1.6~1.8로 제각각 크게 잡혀 있다. 전부 지금 값의 절반으로 줄인다(예: 3→1.5, 4→2, 2.2→1.1, 1.6~1.8→0.8~0.9). 줄인 뒤 실행해서 눈으로 전체 비율이 어색하지 않은지 확인하고, 필요하면 값을 미세 조정한다.
- [x] #17 (완료 2026-09-02) (버그 리포트) 밭에 씨앗을 심으면 플레이어가 식물(새싹/작물) 스프라이트보다 뒤에 렌더링되어 어색하다. 플레이어가 항상 식물보다 앞에 보이도록 z_index/렌더 순서를 고친다 (다른 바닥 장식물과의 z_index 순서를 깨지 않도록 STATUS.md의 기존 z_index 관련 결정 로그를 먼저 확인할 것).
- [x] #18 (완료 2026-09-02) (버그 리포트) 사슴을 구석(월드 경계 등)으로 몰면 도주 상태인데도 움직이지 않고 멈춰버린다. 지금 도주 로직은 플레이어 반대 방향으로만 이동하는데, 그 방향이 이미 월드 경계 밖이면 막혀서 움직일 곳이 없어지는 것으로 보인다. 경계나 장애물에 막히면 다른 방향으로라도 피할 수 있게 고친다.
- [x] #19 (완료 2026-09-02) 사슴의 체력(HP)이 화면에 보이지 않아서 마취탄 포획 타이밍(체력 10% 미만)을 알기 어렵다. 사슴 위에 체력바(또는 숫자)를 표시한다.
- [x] #20 (완료 2026-09-02, 부분 완료 — 상세는 STATUS.md 참고) 전체적으로 도트 그래픽 느낌(코어키퍼/스타듀밸리 스타일)이 부족하다. 확인된 원인 중 하나: 바닥(Ground)이 텍스처 없이 단색 초록 Polygon2D로만 되어 있다 — 실제 도트 타일 텍스처(잔디 등)로 바꾼다. 그 외에도 지금까지 만들어진 스프라이트들을 전체적으로 다시 훑어보고, DESIGN.md "그래픽 파이프라인" 규칙(그림은 반드시 PixelLab로 만들고, 서로 스타일/팔레트가 겉돌지 않게)에 맞게 통일감 있게 다듬는다. #15(사슴 일관성)와 겹치는 부분은 중복 작업하지 말고 그쪽에 맡긴다.
- [x] #21 (완료 2026-09-02) 인벤토리를 슬롯 기반으로 새로 만든다. 지금 `scripts/inventory_data.gd`는 아이템별 개수만 세는 딕셔너리 방식인데, 이걸 슬롯 배열 방식으로 바꾼다: 일반 슬롯 18칸(그중 인덱스 0~8인 맨 위 9칸이 핫바를 겸함), 그와 별개로 장비 슬롯 9칸(모자1/상의1/하의1/신발1/목걸이2/반지2/가방1). 지금 있는 아이템(rice_seed, rice, iron, captured_deer 등 `all_counts()`로 보이는 것들)은 일반 슬롯에 스택으로 들어가도록 마이그레이션한다. E 키를 누르면 인벤토리 창(일반 18칸 + 장비 9칸 그리드)이 열리고 닫힌다. 이번 항목은 슬롯/UI/데이터 구조까지만 만들면 되고, 장비 슬롯에 넣은 아이템의 실제 효과는 만들지 않아도 된다(DESIGN.md 범위 밖).
- [x] #22 (완료 2026-09-02) 도구를 실제 아이템으로 만든다. 총(기존 기본 소총), 도끼, 곡괭이, 낫, 낚싯대 — 이 5개를 인벤토리에 들어가는 아이템으로 만든다(아직 없는 도끼/곡괭이/낫/낚싯대는 새 아이템으로 추가하고 PixelLab로 아이콘/스프라이트를 만든다). 숫자키 1~9로 핫바(#21에서 만든 일반 슬롯의 맨 위 9칸) 중 하나를 고르면 그 칸의 아이템을 플레이어가 손에 든 것으로 표시하고(스프라이트 전환), 이후 좌클릭이 그 도구의 동작을 수행하게 만든다(예: 총이면 기존 발사 로직 그대로, 다른 도구는 다음 항목 #23에서 실제 동작을 연결). 핫바 슬롯이 비어있거나 도구가 아닌 아이템이면 아무것도 들지 않은 빈손 상태로 둔다.
- [x] #23 (완료 2026-09-02) 도구 상호작용을 전부 좌클릭으로 통일한다. 지금 `scenes/farm_plot/farm_plot.gd`, `scenes/resource_point/resource_point.gd`, `scenes/ranch_zone/ranch_zone.gd`가 전부 F키(`KEY_F`)로 상호작용을 처리하고 있는데, 이걸 없애고 #22에서 만든 "지금 손에 든 도구"가 맞는 도구일 때 좌클릭으로 동작하게 바꾼다: 곡괭이를 들고 채광 포인트 앞에서 좌클릭→채광, 낫을 들고 채집 포인트/밭(수확) 앞에서 좌클릭→채집·수확, 도끼는 아직 벌목 대상(나무)이 없으니 좌클릭 애니메이션/동작만 연결해두고 실제 벌목은 만들지 않는다(DESIGN.md 범위 밖), 낚싯대는 낚시 스팟이 아직 없으면 좌클릭 시 최소한의 반응(예: 낚시 시작 애니메이션/상태)만 만들고 실제 낚시 결과물 로직까지는 범위에 넣지 않아도 된다. 목장(ranch_zone)의 F키 상호작용(사슴 넣기 등)도 같은 방식(맞는 도구/빈손 좌클릭)으로 바꾼다.
- [x] #24 (완료 2026-09-02) 사냥/채집/채광 결과물을 바로 인벤토리에 넣지 않고 바닥에 드롭되게 만든다. 사슴을 사냥/포획했을 때, 채집·채광 포인트를 사용했을 때 그 자리에 아이템 드롭 오브젝트(픽업 가능한 스프라이트)가 생기고, 플레이어가 가까이 다가가면(접촉하면) 자동으로 인벤토리(#21의 일반 슬롯)에 들어가면서 사라진다. 좌클릭으로 줍는 방식은 쓰지 않는다(좌클릭은 도구 동작에 이미 쓰이고 있음).
- [x] #25 (완료 2026-09-02, 설계 정정) 곡괭이와 낫을 별도 아이템으로 나눴던 걸 다시 하나(곡괭이낫)로 합친다. #22/#23에서 "총, 도끼, 곡괭이낫, 낚싯대"를 곡괭이/낫 두 아이템으로 잘못 나눴다 — 실제로는 채집+채광을 함께 하는 도구 하나였다(나중에 채광 포인트를 "광산"으로 확장하면서 추가 채집물도 그 안에 놓을 계획이라 도구를 합쳐두는 것, DESIGN.md 참고). 낫 아이템/인벤토리 슬롯을 제거하고, 낫에 연결했던 채집 상호작용을 곡괭이로 옮겨서 곡괭이 하나로 채집 포인트와 채광 포인트 양쪽 다 쓸 수 있게 만든다. 곡괭이 이미지가 이미 곡괭이+낫 형태로 그려져 있다면 새로 그리지 않아도 된다(먼저 확인).
- [x] #26 (완료 2026-09-02) (버그 리포트) 목장(ranch_zone)이 실제 충돌 오브젝트가 아니라서 동물이 경계를 통과한다. 지금은 사슴 위치를 매 프레임 강제로 되돌리는 방식(clamp)으로만 안에 가둬놓고 있는데, 실제 충돌체(담장 등 StaticBody2D+CollisionShape2D)를 목장 경계에 추가해서 물리적으로 못 지나가게 만든다.
- [x] #27 (완료 2026-09-02) (버그 리포트/설계 정정) 농사 심기 방식을 바꾼다. 지금은 도구(곡괭이/낫 등)로 상호작용해서 심어지는 것 같은데, DESIGN.md대로 "심을 씨앗 아이템을 핫바에서 손에 든 상태"에서 밭에 좌클릭해야 심어지도록 바꾼다. 수확은 곡괭이(#25에서 합친 도구)를 든 상태로 좌클릭하면 되도록 유지/확인한다.
- [x] #28 (완료 2026-09-02) (버그 리포트) 총을 쐈을 때 총알이 마우스 커서(조준점)보다 위에서 나가는 것 같다. 실제로 재현해서 총알 스폰 위치/방향 계산을 확인하고, 조준한 지점(또는 조준 방향)과 정확히 맞도록 고친다.
- [x] #29 (완료 2026-09-02) (버그 리포트) 철이나 벼 같은 채집/채광 포인트를 다 캤을 때 회색으로 표시되는데, 이걸 회색으로 바꾸는 대신 쿨다운 동안 완전히 사라졌다가 재생성될 때 다시 나타나도록 바꾼다.
- [x] #30 (완료 2026-09-02, 밸런스) 사슴이 도망갈 때 플레이어 반대 방향으로 일직선으로만 움직여서 맞추기 너무 쉽다. 예측하기 어렵게(지그재그로 방향을 바꾸거나, 무작위 편차를 섞는 등) 도주 로직을 개선해서 사냥 난이도를 올린다.
- [x] #31 (완료 2026-09-02) (버그 리포트) 인벤토리 창(#21)에서 아이템을 마우스로 들어서 다른 슬롯으로 옮길 수 없다. 드래그 앤 드롭으로 슬롯 사이(일반↔일반, 일반↔장비, 핫바 포함) 이동이 되게 만든다. 또한 아이템을 인벤토리 창 바깥으로 드래그해서 놓으면 그 아이템이 인벤토리에서 사라지고 플레이어 근처 바닥에 드롭되게 만든다(버리기) — #24에서 만든 바닥 드롭 아이템 오브젝트를 재사용한다.
- [x] #32 (완료 2026-09-02, 버그 리포트) 총을 든 채로 그 총을 버리면(#31의 드래그 버리기) 여전히 총을 들고 있고 심지어 발사도 된다. 원인 확인됨: `scenes/world/world.gd`의 `_select_hotbar()`는 숫자키를 누를 때만 `_held_tool`/손 스프라이트를 갱신하고, 인벤토리 내용이 바뀌는 것(드래그로 슬롯이 비워지는 등)에는 반응하지 않는다. 인벤토리가 바뀔 때마다(`InventoryData`의 변경 신호 등을 이용해) 현재 선택된 핫바 슬롯을 다시 확인해서, 그 슬롯이 비었거나 도구가 아니면 빈손 상태로 되돌리도록 고친다.
- [x] #33 (완료 2026-09-02) 총에 탄창/재장전을 추가한다. DESIGN.md대로 탄창 용량 8발, 다 쏘면 좌클릭해도 발사되지 않고 R키를 누르면 재장전된다(약간의 시간 소요, 예비 탄약 제한 없이 항상 가득 재장전). 탄창에 남은 발수를 화면에 표시한다.
- [x] #34 (완료 2026-09-02, 설계 정정) 목장에 동물을 풀어놓는 방식을 바꾼다. 지금은 빈손 상태로 목장 구역에 좌클릭하면 풀어놓아지는데, DESIGN.md대로 포획한 동물 아이템(captured_deer)을 핫바에서 손에 든 상태로 목장 구역에 좌클릭해야 풀어놓아지도록 바꾼다(씨앗 심기와 같은 "든 것으로 좌클릭" 패턴). 아이템을 사용하면 인벤토리에서 1개 소비된다.
- [x] #35 (완료 2026-09-02, 품질 피드백) 도구를 들고 있을 때 플레이어가 4방향(정면/좌/우/후면)을 볼 때 전부 부자연스럽다. 원인으로 보이는 것 하나: 손에 든 도구 스프라이트(`_held_item_sprite`)가 `player_sprite`의 자식 노드라서 항상 플레이어 스프라이트보다 위에 그려진다 — 뒤(north)를 보고 있을 때도 도구가 몸 앞에 떠 있는 것처럼 보일 수 있다. 4방향 전부(도구 든 상태) 실제로 스크린샷을 찍어서 직접 눈으로 확인하고, 방향별로 도구가 몸 앞/뒤 어디에 그려져야 자연스러운지 판단해서 z_index나 위치/각도를 방향별로 다르게 조정해 고친다.
- [x] #36 (완료 2026-09-03, 버그 리포트) 기본탄과 마취탄이 탄창(잔여 발수)을 공유하고 있다. DESIGN.md대로 두 탄종의 탄창을 완전히 분리한다 — 기본탄을 다 쓰고 우클릭으로 마취탄으로 바꾸면 마취탄 탄창은 그대로 가득 차 있어야 한다(또는 마취탄도 이미 일부 썼다면 그만큼만 줄어 있어야 함). R 재장전은 지금 들고 있는 탄종의 탄창만 채운다.
- [x] #37 (완료 2026-09-03, 품질 피드백, #35 재지적) 도구를 들고 있을 때 여전히 이상하다 — #35에서 아이콘 위치/z_index 조정만으로 고쳤는데 그걸로는 부족했다. DESIGN.md "도구 동작 표현" 규칙대로, **총**의 "들고 있는 모션"과 "발사하는 모션"을 서로 다른 그림/애니메이션으로 PixelLab로 새로 만들어서 자연스럽게 고친다(단순 오프셋 조정이 아니라 실제 그림 작업). 4방향 전부 스크린샷으로 확인한다. 이번 항목은 총만 다루고, 도끼/곡괭이낫/낚싯대는 다음 항목들에서 각각 처리한다.
- [x] #38 (완료 2026-09-03, 품질 피드백) DESIGN.md "도구 동작 표현" 규칙대로, **도끼**의 "들고 있는 모션"과 "(나무를) 패는 모션"을 서로 다른 그림/애니메이션으로 PixelLab로 만든다(아직 벌목 대상인 나무가 없으니 실제 나무를 패는 상황 없이도, 좌클릭 시 패는 동작이 자연스럽게 보이도록만 만들면 된다). 4방향 스크린샷으로 확인한다.
- [x] #39 (완료 2026-09-03, 품질 피드백) DESIGN.md "도구 동작 표현" 규칙대로, **곡괭이낫**의 "들고 있는 모션", "채광하는 모션", "채집하는 모션"을 각각 서로 다른 그림/애니메이션으로 PixelLab로 만든다(채광과 채집은 같은 도구를 쓰지만 동작 자체는 다르게 표현). 4방향 스크린샷으로 확인한다.
- [x] #40 (완료 2026-09-03) DESIGN.md "도구 동작 표현" 규칙대로, **낚싯대**의 "들고 있는 모션"과 "낚시하는 모션"을 서로 다른 그림/애니메이션으로 PixelLab로 만든다(아직 낚시 스팟/실제 낚시 결과 로직이 없어도, 좌클릭 시 낚시하는 동작 자체는 자연스럽게 보이도록 만들면 된다). 4방향 스크린샷으로 확인한다.
- [x] #41 (완료 2026-09-03, 구조 개편, 최우선) 캐릭터를 `Sprite2D`(방향별 정지 이미지 한 장)에서 `AnimatedSprite2D`(`SpriteFrames`)로 바꾼다. DESIGN.md "캐릭터 애니메이션" 규칙대로, 맨몸(도구 없이 빈손) 기준으로 방향별 idle 애니메이션과 walk 애니메이션(4~6프레임)을 PixelLab로 만들어서 실제로 걷는 동안 다리가 움직이는 것처럼 보이게 만든다. 지금 있는 캐릭터 색상(blue/green/red) 전부에 적용한다. 이번 항목은 도구 없는 상태까지만 하고, 도구 통합은 다음 항목들(#42~#45)에서 각각 처리한다. `_held_item_sprite` 오프셋 방식(#22~#40에서 쓰던 방식)은 이번 항목에서 아직 없애지 않아도 된다 — 도구별 통합이 끝난 뒤(#45 이후) 한꺼번에 정리한다.
- [x] #42 (완료 2026-09-03) 총을 #41의 애니메이션 프레임에 통합한다. "들고 있는(idle)" 모션과 "발사하는" 모션 프레임 자체에 캐릭터가 총을 쥔 손 모양을 그려 넣어서(별도 아이콘 오버레이가 아니라 캐릭터 그림 자체에 포함), 손과 총이 자연스럽게 붙어 보이게 만든다. 4방향 전부 스크린샷으로 확인한다. 기존 `_held_item_sprite`로 총 아이콘을 띄우던 코드는 이 항목에서 총에 한해 제거하고 새 애니메이션으로 교체한다.
- [x] #43 (완료 2026-09-03) 도끼를 #41의 애니메이션 프레임에 통합한다(#42와 같은 방식) — "들고 있는" 모션과 "패는" 모션. 손과 도끼가 멀리 떨어져 보이지 않게, 실제로 쥔 것처럼 그린다.
- [x] #44 (완료 2026-09-03) 곡괭이낫을 #41의 애니메이션 프레임에 통합한다(#42와 같은 방식) — "들고 있는" 모션, "채광하는" 모션, "채집하는" 모션 세 가지.
- [x] #45 (완료 2026-09-03, 재오픈분 재작업) 낚싯대를 #41의 애니메이션 프레임에 통합한다(#42와 같은 방식) — "들고 있는" 모션과 "낚시하는" 모션. 이 항목을 끝으로 모든 도구 통합이 끝나면, 더 이상 쓰이지 않는 `_held_item_sprite`/`HELD_ITEM_OFFSETS`/`HELD_ITEM_BEHIND_FACINGS` 관련 코드를 정리(삭제)한다.
- [x] #46 (완료 2026-09-03, QA 관찰 — 결함 2건 발견, #47/#48로 티켓 발행, #49로 재확인 루프 등록) [QA 관찰] DESIGN.md "QA 관찰 항목" 형식을 그대로 따른다. **범위: 총을 든 캐릭터의 4방향(정면/좌/우/후면) idle 모습과 발사(공격) 모션.** 실제로 스크린샷을 찍어서(STATUS.md의 기존 헤드리스 캡처/마우스 조준 강제 우회법을 먼저 확인해서 재사용) Read 도구로 이미지를 직접 열어 눈으로 본다. 스타듀밸리/코어키퍼 수준과 비교해서 이상한 점(자세가 뻣뻣하다, 총과 손이 안 맞는다, 방향별로 비율이 다르다, 발사 순간 부자연스럽다 등)을 구체적으로 찾는다. 문제를 찾으면 각각 새 INBOX 항목으로 추가하고, 그 뒤에 이 항목과 똑같은 내용의 "QA 재확인" 항목을 새 번호로 하나 더 추가한다(재검증 루프가 이어지도록). 문제를 하나도 못 찾으면 새 항목을 추가하지 말고 DESIGN.md에 "총 들기/발사 모션 품질 확인 완료(날짜)"라고 기록한다. 이 항목 자체에서는 그림을 새로 만들거나 코드를 고치지 않는다 — 보고 판정하고 티켓을 발행하는 것까지만 한다.
- [x] #47 (완료 2026-09-03, 버그 리포트, #46 QA에서 발견) 총을 들고 **동쪽 또는 서쪽**을 볼 때, 발사 모션(`gun_fire_east`/`gun_fire_west`)의 총구 불꽃이 총구(캐릭터가 바라보는 방향 쪽 총 끝)가 아니라 **총의 반대쪽 끝(몸에 더 가까운 쪽)에서** 나타난다. 예를 들어 동쪽을 볼 때 총구는 화면 오른쪽(캐릭터가 향한 방향)에 있는데, 발사 불꽃은 화면 왼쪽(몸 쪽, 캐릭터 등 뒤 방향)에서 튄다 — 마치 총이 아니라 캐릭터 몸에서 반대 방향으로 불이 나는 것처럼 보여 부자연스럽고 조준 방향과 모순된다. (참고: 남/북 방향(`gun_fire_south`/`gun_fire_north`)은 불꽃이 총구 쪽(바깥쪽 끝)에 정상적으로 나타나 동/서만의 문제로 보인다.) `game/assets/sprites/character/gun/{variant}_east_fire.png`/`{variant}_west_fire.png`(3색)에서 불꽃 위치를 실제 총구 쪽으로 옮겨서 다시 생성/합성한다. 4방향(특히 동/서) 재확인은 스크린샷으로 직접 눈으로 검증한다.
- [x] #48 [DESIGN] (2026-09-03 무효화 — 대상 자산 삭제됨, #50 이후로 대체) (버그 리포트, #46 QA에서 발견) 총을 들고 **남쪽(정면) 또는 북쪽(후면)**을 볼 때, 총이 캐릭터가 실제로 조준하는 방향(화면 아래쪽/위쪽)과 무관하게 몸 앞에 완전히 수평(좌우)으로 걸쳐 들려 있다 — 마치 총을 옆으로 눕혀 몸에 걸치고 있는 것처럼 보이고, 아래/위를 겨누고 있다는 느낌이 전혀 들지 않는다. 반면 동/서(옆모습) 방향은 총이 실제로 보고 있는 쪽으로 자연스럽게 뻗어 있어 문제가 없다. DESIGN.md "전투 감각의 기준은 덕코프"에 맞춰, 남/북 방향에서도 총이 조준 방향(아래쪽/위쪽)으로 기울어져 보이도록 `gun_idle_south`/`gun_fire_south`/`gun_idle_north`/`gun_fire_north`(3색, 총 12장)를 다시 그린다. 4방향 스크린샷으로 재확인한다.
- [x] #49 (2026-09-03 무효화 — 대상 자산 삭제됨, 새 자산 완성 후 새 QA 항목으로 재등록 예정) [QA 재확인] DESIGN.md "QA 관찰 항목" 형식을 그대로 따른다. **범위: 총을 든 캐릭터의 4방향(정면/좌/우/후면) idle 모습과 발사(공격) 모션.** (#46과 동일한 내용 — #47/#48이 고쳐졌는지 재검증하는 루프) 실제로 스크린샷을 찍어서(STATUS.md의 기존 헤드리스 캡처/마우스 조준 강제 우회법을 먼저 확인해서 재사용) Read 도구로 이미지를 직접 열어 눈으로 본다. 스타듀밸리/코어키퍼 수준과 비교해서 이상한 점을 구체적으로 찾는다. 문제를 찾으면 각각 새 INBOX 항목으로 추가하고, 그 뒤에 이 항목과 똑같은 내용의 "QA 재확인" 항목을 새 번호로 하나 더 추가한다. 문제를 하나도 못 찾으면 새 항목을 추가하지 말고 DESIGN.md에 "총 들기/발사 모션 품질 확인 완료(날짜)"라고 기록한다. 이 항목 자체에서는 그림을 새로 만들거나 코드를 고치지 않는다.

- [x] #50 (완료 2026-09-03) [DESIGN] 캐릭터 기본 idle/walk 애니메이션을 SpriteCook으로 새로 만든다(자산 초기화 후 재작업, 기준 색상 green만 먼저). DESIGN.md "그래픽 파이프라인"대로 SpriteCook의 캐릭터 애니메이션 기능(idle/walk 등)을 먼저 조사해서 쓴다. 방향 4개(south/east/north/west) × idle(1프레임 이상)/walk(여러 프레임) 애니메이션을 만들어서 `world.gd`의 `_build_player_sprite_frames()`/`_current_animation_name()`에 walk_<방향> 분기를 추가한다(idle_<방향>은 이미 있음, 기존 정지 이미지 재사용 가능하면 재사용해도 됨). 걷는 동안 실제로 다리가 움직이는 것처럼 보여야 한다 — 이번 디자인 작업에서 자연스러움과 퀄리티가 최우선이다. 4방향 전부 스크린샷으로 직접 확인한다.
- [x] #51 (완료 2026-09-03) [DESIGN] #50의 걷기 애니메이션을 나머지 색상(blue/red)까지 확장한다. green과 같은 방식/품질로 만들고 4방향 스크린샷으로 확인한다.
- [x] #52 (완료 2026-09-03) [DESIGN] 총을 든 캐릭터의 "들고 있는" 모션과 "발사하는" 모션을 SpriteCook으로 새로 만든다(기준 색상 green만 먼저). DESIGN.md "도구 동작 표현"대로 캐릭터 애니메이션 프레임 자체에 총을 쥔 손 모양을 그려 넣는다(별도 아이콘 오버레이 아님). `_build_player_sprite_frames()`에 gun_idle_<방향>/gun_fire_<방향> 애니메이션을 추가하고 `_current_animation_name()`의 `_held_tool == "gun"` 분기를 되살린다. 이번엔 특히 신경써서: 남/북(정면/후면)에서도 총이 조준 방향(위/아래)으로 자연스럽게 기울어져 보이게 만들 것 — 예전에 이 부분이 부자연스럽다는 지적을 받았었다(무효화된 #48 참고). 4방향 스크린샷으로 직접 확인한다.
- [x] #53 (완료 2026-09-03) [DESIGN] #52의 총 모션을 나머지 색상(blue/red)까지 확장한다.
- [x] #54 (완료 2026-09-03) [DESIGN] 곡괭이낫을 든 캐릭터의 "들고 있는"/"채광하는"/"채집하는" 세 모션을 SpriteCook으로 새로 만든다(기준 색상 green만 먼저). `_build_player_sprite_frames()`에 pickaxe_idle_<방향>/pickaxe_mining_<방향>/pickaxe_gathering_<방향>을 추가하고 `_current_animation_name()`의 `_held_tool == "pickaxe"` 분기를 되살린다. 곡괭이질하는 동작이 실제로 캐고 있는 것처럼 자연스럽게 보이도록 특히 신경 쓴다. 4방향 스크린샷으로 직접 확인한다.
- [x] #55 (완료 2026-09-03) [DESIGN] #54의 곡괭이낫 "들고 있는"/"채광하는"/"채집하는" 모션을 나머지 색상(blue/red)까지 확장한다. green과 같은 방식/품질로 만들고 4방향 스크린샷으로 확인한다.
- [x] #56 (완료 2026-09-03, QA 전체 스윕 — 결함 4건 발견, #57~#62로 티켓 발행) [QA] 전체 스윕: 메인 메뉴부터 모든 시스템을 실제로 플레이해보며 문제를 찾는다 (BUILD/DESIGN 완료 카운터가 5에 도달해 자동 등록됨 — #51~#55).
- [x] #57 (완료 2026-09-03) [DESIGN] (버그 리포트, #56 QA에서 발견) 캐릭터가 도끼를 손에 들었을 때 도끼가 화면에 전혀 보이지 않는다(맨손 idle 그대로). 원인: 자산 초기화(바퀴 64 부근) 때 `#43`이 만들었던 `axe_idle_*`/`axe_chop_*` 애니메이션 자산과 `world.gd`의 관련 코드가 모두 삭제됐는데, 이후 걷기(#50/51)·총(#52/53)·곡괭이낫(#54/55)은 각각 재작업 항목이 다시 등록됐지만 도끼는 재작업 항목이 등록되지 않고 누락됐다(코드에는 "axe_idle_*/axe_chop_*"를 언급하는 주석만 남아있고 실제 애니메이션 빌드 로직은 없음 — `scenes/world/world.gd`의 `_build_player_sprite_frames()`/`_current_animation_name()`에 gun/pickaxe와 달리 axe 분기가 아예 없다). 총(#52)/곡괭이낫(#54)과 같은 방식(SpriteCook `generate-sync`+`edit_asset_id`)으로 도끼의 "들고 있는"/"패는" 모션을 green 기준으로 새로 만들고, `_build_player_sprite_frames()`/`_current_animation_name()`에 `_held_tool == "axe"` 분기(axe_idle_<방향>/axe_chop_<방향>, `ResourceLoader.exists()` 가드 패턴)를 되살린다. 4방향 스크린샷으로 직접 확인한다.
- [x] #58 (완료 2026-09-03) [DESIGN] #57의 도끼 모션을 나머지 색상(blue/red)까지 확장한다. #57과 같은 방식/품질로 만들고 4방향 스크린샷으로 확인한다.
- [x] #59 (완료 2026-09-03) [DESIGN] (버그 리포트, #56 QA에서 발견) 캐릭터가 낚싯대를 손에 들었을 때 낚싯대가 화면에 전혀 보이지 않는다(맨손 idle 그대로) — #57(도끼)과 원인이 같다(자산 초기화 이후 `fishing_rod_idle_*`/`fishing_rod_fishing_*` 재작업 항목이 등록되지 않고 누락됨, `world.gd`에 fishing_rod 분기 없음). 총(#52)/곡괭이낫(#54)과 같은 방식으로 낚싯대의 "들고 있는"/"낚시하는" 모션을 green 기준으로 새로 만들고, `_build_player_sprite_frames()`/`_current_animation_name()`에 `_held_tool == "fishing_rod"` 분기를 되살린다. 4방향 스크린샷으로 직접 확인한다.
- [x] #60 (완료 2026-09-03) [DESIGN] #59의 낚싯대 모션을 나머지 색상(blue/red)까지 확장한다. #59와 같은 방식/품질로 만들고 4방향 스크린샷으로 확인한다.
- [x] #61 (완료 2026-09-03) [DESIGN] (품질 피드백, #56 QA에서 발견) 바닥 잔디 텍스처(`assets/sprites/ground/grass_tile.png`, 32x32)를 실제 게임 화면에서 확대해 보면, 자연스러운 잔디 질감이 아니라 작은 숫자/기호처럼 보이는 형태가 격자 모양으로 반복되는 이상한 패턴이 뚜렷하게 보인다(타일을 이어붙였을 때 화면 전체에 이 패턴이 규칙적으로 반복돼 매우 눈에 띈다 — 스타듀밸리/코어키퍼의 자연스러운 잔디 텍스처와 확연히 다르다). 이 텍스처를 새로 생성해서 교체한다. STATUS.md(바퀴 23)의 "이음매 없는 반복 타일" 관련 결정 로그(`/create-tileset` Wang 타일셋 엔드포인트 사용법, `create-image-pixflux`는 이음매가 생겨서 부적합했다는 기록)를 먼저 확인하고, 이번엔 생성된 타일을 실제 게임 화면(여러 번 반복 타일링된 상태)에서 확대 스크린샷으로 직접 확인해서 이런 반복 아티팩트가 없는지 검증한 뒤 교체한다.
- [x] #62 (완료 2026-09-03) [BUILD] (버그 리포트, #56 QA에서 발견) 인벤토리 창 하단 핫바 UI에서, 아이템 이름이 4글자 이상인 도구(예: "곡괭이낫")가 핫바 슬롯에 있으면 슬롯 좌상단의 번호 라벨("3")과 가운데 정렬된 아이템 이름 라벨이 서로 겹쳐서 "3곡괭이낫"처럼 붙어 보인다(다른 슬롯은 "1 총", "2 도끼", "4 낚싯대"처럼 번호와 이름 사이 간격이 있어 안 겹친다). `scenes/world/world.gd`의 `_make_hotbar_cell()`/`_refresh_hotbar()`에서 슬롯 크기(56x56)에 비해 긴 아이템 이름이 번호 라벨과 겹치지 않도록 폰트 크기를 줄이거나 줄바꿈/축약 처리, 또는 번호 라벨 위치를 조정한다.
- [x] #63 (완료 2026-09-03, QA 전체 스윕 — 결함 1건 발견, #64로 티켓 발행) [QA] 전체 스윕: 메인 메뉴부터 모든 시스템을 실제로 플레이해보며 문제를 찾는다 (BUILD/DESIGN 완료 카운터가 5에 도달해 자동 등록됨 — #57~#61). `game/qa/full_sweep.gd`를 재사용할 수 있다(단, 도끼/낚싯대 도구 애니메이션과 잔디 텍스처가 이번에 바뀌었으니 그 부분은 새로 확인할 것 — 특히 잔디 텍스처가 64x64로 커졌으므로 기존 32x32 가정이 남아있는 코드/문서가 없는지도 확인).
- [x] #64 (완료 2026-09-03) [BUILD] (버그 리포트, #63 QA 전체 스윕에서 발견) 채집/채광 포인트(`scenes/resource_point/resource_point.gd`)의 `Sprite` 노드에 `z_index`가 지정돼 있지 않다(기본값 0, 플레이어와 동일) — 그래서 실제 렌더링 순서가 씬 트리 순서에 따라 결정되고, 플레이어의 화면상 위치(더 아래=더 앞)와 무관하게 뒤집힐 수 있다. 실제로 재현됨: (1) 채집 포인트(벼) 앞에 선 플레이어가 그 포인트의 롱 스프라이트(벼 이삭)에 완전히 가려져 머리가 안 보인다 — `scenes/farm_plot/farm_plot.gd`가 이미 INBOX #17에서 겪은 것과 같은 종류의 결함인데, `farm_plot.tscn`/`ranch_zone.tscn`은 `z_index = -1`을 줘서 고쳤지만 `resource_point`(별도 .tscn 없이 코드로 구성)는 그 처리가 빠졌다. (2) 그 결과 한 채집 포인트의 안내 문구(`Prompt` 라벨, 예: "곡괭이낫을 들고 좌클릭: 채집 (벼 씨앗)")가 근처의 다른 채광 포인트(돌) 스프라이트에 가려 텍스트 중간이 잘려 보인다(스크린샷으로 확인, `/tmp/qa63/15_gathering_point.png`). `farm_plot`/`ranch_zone`과 같은 방식으로 `resource_point.gd`의 `Sprite`에 `z_index = -1`(또는 동일한 값)을 지정해 플레이어보다 항상 뒤에 그려지도록 고친다. 고친 뒤 채집/채광 포인트 두 종류 모두 실제 화면 스크린샷으로 재확인할 것.

- [x] #65 (완료 2026-09-03) [BUILD] (버그 리포트, 사용자 직접 확인) 도구(총/도끼/곡괭이낫/낚싯대)를 든 채로 이동하면 걷기 애니메이션이 재생되지 않고 idle/사용 포즈로 얼어붙은 채 미끄러지듯 움직인다. 원인 확인됨: `scenes/world/world.gd`의 `_current_animation_name()`이 `_held_tool`이 있으면 `gun_idle_<방향>`/`pickaxe_idle_<방향>` 등을 무조건 반환하고 `_is_moving`을 전혀 확인하지 않는다(맨손일 때만 `walk_<방향>`/`idle_<방향>`을 고름). 지금은 도구를 든 채 걷는 전용 그림(`gun_walk_*` 등)이 없으므로, DESIGN.md "완성의 기준"(2026-09-03 조정, 기본만 지키면 됨)에 따라 **새 그림을 만들지 않고**: 도구를 들고 있어도 `_is_moving`이 true면 맨손 `walk_<방향>` 애니메이션을 재생하도록 고친다(도구를 쥔 손 모양은 이동 중엔 잠깐 안 보여도 된다 — 정지하면 다시 도구 idle 포즈로 돌아옴). 4방향 모두 실제로 이동시켜서 다리가 움직이는지 스크린샷으로 확인한다.
- [x] #66 (완료 2026-09-03) [DESIGN] (버그 리포트, 사용자 직접 확인) 총을 든 캐릭터의 방향별 자세가 일관되지 않는다. 확인됨: `green_south_idle.png`와 `green_south_fire.png`가 사실상 동일하게 총을 아래로 늘어뜨린 자세라 남/북을 보고 쏴도 자세 변화가 안 보인다. 반면 `green_east_idle.png`는 이미 총을 어깨에 대고 조준하는 자세라, idle인데도 마치 발사 중인 것처럼 보인다. DESIGN.md "완성의 기준"(2026-09-03 조정)의 "기본" 수준으로 다시 만든다: 모든 방향에서 idle=편하게 든 자세, fire=조준하고 쏘는 자세로 최소한 구별되게, 그리고 방향 4개의 idle 기본 자세가 서로 크게 어긋나지 않게. 정교한 디테일까지는 필요 없다. green 기준색으로 먼저 고치고 스크린샷(4방향 idle+fire, 총 8장)으로 직접 확인한 뒤, 통과하면 blue/red로 확장하는 건 별도 항목으로 쪼갠다.
- [x] #67 (완료 2026-09-03, QA 디자인 관점 전체 스윕 — 결함 2건 발견, #68~#69로 티켓 발행) [QA] 전체 스윕이지만 **디자인(시각적 품질) 관점**으로 진행한다. `game/qa/full_sweep.gd`(이미 있는 재사용 캡처 스크립트)로 메인 메뉴부터 캐릭터 생성/커스터마이징/월드/도구 4종(총/도끼/곡괭이낫/낚싯대)의 idle·사용·이동 상태/인벤토리/농사·채집·채광·목장/낮밤·날씨까지 전부 스크린샷을 찍고, 이번엔 기능이 동작하는지가 아니라 **그림·애니메이션이 자연스러운가**를 DESIGN.md "완성의 기준"(2026-09-03 조정, 기본만 지키면 됨 — 상태 누락 없음/idle-사용 구별/방향 간 큰 어긋남 없음)에 비춰 하나하나 판정한다. 문제를 찾으면 원인까지 확인해서 정확한 태그([BUILD]는 코드만으로 고칠 수 있는 것, [DESIGN]은 그림을 다시 만들어야 하는 것)를 붙여 새 항목으로 등록한다. 그 외에 "고치면 좋을 것 같다"는 개선 아이디어(필수 결함은 아니지만)가 보이면, 새 항목으로 무작정 등록하지 말고 `docs/STATUS.md`의 "디자인 개선 후보(사용자 검토용)" 섹션에 한 줄씩 추가한다(이 섹션은 대시보드에 표시되고 사용자가 직접 검토함). 이 항목 자체는 그림/코드를 고치지 않는다 — 관찰과 티켓 발행까지만.
- [x] #68 (완료 2026-09-03) [BUILD] (버그 리포트, #67 QA 디자인 스윕에서 발견) 월드에 배치된 오브젝트(채광 포인트의 돌 스프라이트, 목장 구역의 상호작용 안내 문구 Label 등)가 화면상 위치가 겹치면 UI(CanvasLayer, 좌상단 탄약/인벤토리 패널 등)보다 앞에 그려진다 — 원래는 CanvasLayer UI가 항상 월드 콘텐츠보다 위에 그려져야 한다. 재현 1: `game/qa/full_sweep.gd`의 `farm_empty`/`farm_growing`/`farm_ready` 스텝(스크린샷 `/tmp/qa67/21~23_*.png`, 재실행하려면 project.godot [autoload]에 이 스크립트를 임시로 추가하고 `godot --path .`로 실행)에서, 근처에 무작위로 스폰된 채광 포인트의 돌(`scenes/resource_point/resource_point.gd`의 `Sprite`, `mining_point.tscn`에서 `z_index = -1`로 지정됨)이 좌상단 인벤토리 텍스트 패널(`UI/HUD/InventoryPanel/InventoryLabel`, `world.tscn`에서 `CanvasLayer` 자식)을 완전히 가려 "벼" 글자 일부가 안 보인다. 재현 2: `ranch_zone` 스텝(`/tmp/qa67/26_ranch_zone.png`)에서는 목장 구역의 상호작용 안내 Label(`scenes/ranch_zone/ranch_zone.tscn`의 `Prompt`, z_index 지정 없음=기본값 0)이 같은 인벤토리 패널 위에 반투명하게 겹쳐 그려진다. 서로 다른 종류의 월드 노드(Sprite2D/Label)이고 z_index 값도 다른데(-1과 0) 공통적으로 CanvasLayer UI보다 앞에 그려졌다는 점에서, 개별 노드의 z_index 문제가 아니라 `scenes/world/world.tscn`의 `UI` CanvasLayer(layer 속성 미지정=기본값 1)와 월드 콘텐츠(기본 캔버스, layer 0) 사이의 렌더링 순서 자체에 원인이 있을 가능성이 크다(Godot 4.7.2, GL Compatibility 렌더러 — `project.godot`의 `renderer/rendering_method="gl_compatibility"`). 원인을 코드로 직접 조사해서 고친다. 고친 뒤에는 채광 포인트나 목장 프롬프트가 화면 좌상단 HUD 영역과 겹치는 상황을 다시 재현해(플레이어를 그 근처로 이동시키면 됨) 스크린샷으로 UI가 항상 위에 그려지는지 재확인할 것.
- [x] #69 (완료 2026-09-03) [DESIGN] (품질 피드백, #67 QA 디자인 스윕에서 발견) 도끼 패는 모션 스프라이트 `game/assets/sprites/character/axe/green_south_chop.png` 한 장에, 캐릭터나 도끼 어디에도 연결되지 않은 가늘고 끊어진 곡선(낙서 같은 아티팩트)이 머리 위쪽 왼편에 붕 떠서 그려져 있다(스크린샷 `/tmp/qa67/11_tool_axe_use.png`에서 확인, 원본 파일을 8배 확대해도 동일하게 보임). 같은 배치로 생성된 나머지 방향(north/east/west)과 blue/red 색상의 `*_chop.png` 8장에는 이런 게 전혀 없고(도끼날 근처에 작은 충격/스윙 모션선만 있음, 정상), 대응하는 `green_south_idle.png`에도 없다 — 이 파일 한 장에서만 나타나는 일회성 생성 아티팩트로 판단된다. 이 파일만 다시 생성해서 교체한다(다른 방향/색상은 문제없으니 손대지 않음, 다른 도구도 손대지 않음). 교체 후 idle과 자연스럽게 이어지는지, 낙서 없이 깔끔한지 확대 스크린샷으로 직접 재확인할 것.

- [x] #70 (완료 2026-09-03) [DESIGN] (버그 리포트, 사용자 직접 확인) 걷기(walk) 애니메이션 프레임에서 캐릭터가 idle보다 눈에 띄게 크게 그려진다. 확인됨: `assets/sprites/character/green_south.png`(idle)와 `assets/sprites/character/walk/green_south_walk_0.png`는 캔버스 크기가 동일(68×68)한데 walk 프레임 쪽 캐릭터가 더 크다 — 서로 다른 생성 호출이라 확대 비율이 안 맞은 것으로 보인다(DESIGN.md "캐릭터 애니메이션" 절의 새 크기 일관성 규칙 참고). green 기준으로 걷기 프레임 8장(4방향×idle과 비슷한 크기로) 다시 맞추고, idle과 나란히 놓고 크기를 직접 비교해서 확인한다. 통과하면 blue/red 확장은 별도 항목으로 쪼갠다.
- [x] #71 (완료 2026-09-03, 재현 안 됨 — 상세는 STATUS.md 참고) [DESIGN] (버그 리포트, 사용자 직접 확인) 총을 든 캐릭터의 idle 자세에서 동쪽과 서쪽의 캐릭터 크기가 서로 다르다. 확인됨: `assets/sprites/character/gun/green_east_idle.png`와 `green_west_idle.png`는 캔버스 크기가 같은데(83×81) 실제 캐릭터가 차지하는 크기가 다르다(동쪽이 더 크게 그려짐). 두 방향 중 하나를 기준으로 잡아 나머지를 그 크기에 맞춰 다시 만든다. 다른 도구(도끼/곡괭이낫/낚싯대)의 동/서도 같은 문제가 있는지 확인해서 있으면 같이 기록만 해두고(이번 항목 범위는 총으로 한정), 없으면 넘어간다.
- [x] #72 (완료 2026-09-03) [DESIGN] (버그 리포트, 사용자 직접 확인) 총을 들고 정면(남쪽)을 볼 때 조준/발사 자세가 부자연스럽다 — 총을 몸 앞에 뻣뻣하게 가로로 안고 있는 것처럼 보여서 조준하는 느낌이 안 든다. 참고로 후면(북쪽)은 총을 들어올린 자세가 자연스럽게 나온다(비교 기준으로 삼을 것). DESIGN.md "완성의 기준"(기본만 지키면 됨)은 이미 통과하지만, 이 항목은 정면 자세 자체를 더 자연스럽게(견착하고 조준하는 느낌이 들도록) 다시 만드는 게 목적이다. green 기준으로 `gun_idle_south`/`gun_fire_south`를 다시 만들고 스크린샷으로 확인한다.
- [x] #73 (완료 2026-09-03) [BUILD] (버그 리포트, 사용자 직접 확인) 곡괭이낫을 든 상태에서 채집/채광 대상 없이 허공에 좌클릭하면 아무 반응(모션)이 없다. 원인 확인됨: `world.gd`는 도끼의 패기 모션을 `_unhandled_input()`에서 좌클릭 시 무조건 재생하는데, 곡괭이낫의 `play_pickaxe_use()`는 `resource_point.gd`가 실제 대상과 충돌했을 때만 호출된다. DESIGN.md "도구 동작 표현"(총/도끼/곡괭이낫은 대상 유무와 무관하게 항상 사용 모션이 나와야 함 — 근접무기 확장 계획 근거)에 따라, 곡괭이낫도 도끼와 같은 방식으로 좌클릭하면 대상이 없어도 항상 모션이 나가게 고친다(실제 채광/채집 판정 로직 자체는 그대로 두고, 모션 재생만 좌클릭 이벤트에서 직접 트리거하도록 분리).
- [x] #74 (완료 2026-09-03, QA 전체 스윕 — 결함 1건 발견, #75로 티켓 발행) [QA] 전체 스윕: 메인 메뉴부터 모든 시스템을 실제로 플레이해보며 문제를 찾는다 (BUILD/DESIGN 완료 카운터가 5에 도달해 자동 등록됨 — #68~#72). `game/qa/full_sweep.gd`를 재사용할 수 있다. 이번 구간에서 바뀐 것들을 특히 확인할 것: 채집/채광 포인트 z_index(#68), 도끼 패기 낙서 아티팩트 제거(#69), 걷기 애니메이션 4방향 크기(#70), 총 든 idle 동/서 크기(#71, 재현 안 됨으로 종료됐지만 다시 한 번 봐도 됨), 총 남쪽 조준/발사 자세(#72, 새로 만든 어깨에 견착하는 자세). 기능 동작과 그림/애니메이션 자연스러움 둘 다 본다.
- [x] #75 (완료 2026-09-03, 재진단 — CanvasLayer 순서 문제가 아니라 패널 반투명이 원인이었음) [BUILD] (버그 리포트, #74 QA 전체 스윕에서 발견) #68이 채집/채광 포인트(`resource_point.gd`)의 `Sprite`에 `z_index = -1`을 줘서 "월드 오브젝트가 UI보다 앞에 그려지는" 문제를 고쳤다고 완료 처리됐지만, **다른 종류의 월드 오브젝트에서 같은 증상이 재현된다** — UI `CanvasLayer`(`world.tscn`의 `UI`, `layer = 10`)가 항상 월드보다 위에 그려져야 하는데, 실제로는 화면 위치가 겹치면 여전히 뚫린다. 재현 1: `game/qa/full_sweep.gd`의 `farm_empty`/`farm_ready` 스텝(재실행하려면 `project.godot`의 `[autoload]`에 `QASweep="*res://qa/full_sweep.gd"`를 임시로 추가하고 `godot --path .`로 실행, `game/` 디렉터리에서 실행할 것)에서, 화면 좌상단 근처를 배회하던 사슴(`scenes/deer/deer.gd`, `z_index` 지정 없음=기본값 0)의 목/머리가 좌상단 "인벤토리: ..." HUD 텍스트 패널 위에 그대로 그려진다(픽셀 비교로 확인: 사슴의 갈색 픽셀이 패널의 어두운 초록 배경 위에 나타남, 텍스트를 가림). 재현 2: `mining_point` 스텝에서, 화면 하단에 있던 밭(`scenes/farm_plot/farm_plot.gd`, `Soil`/`Crop` 둘 다 `z_index = -1`)의 나무 울타리 테두리가 핫바 8번 슬롯의 좌상단 모서리 위에 그려진다. 두 경우 모두 서로 다른 노드 타입(AnimatedSprite2D/Sprite2D)과 z_index 값(0과 -1)인데 공통적으로 CanvasLayer(layer=10) UI보다 앞에 그려졌다는 점에서, 개별 노드의 z_index 문제가 아니라 `layer=10` 설정 자체가 이 렌더러(Godot 4.7.2, `gl_compatibility`)에서 화면상 위치/겹침 조건에 따라 완전히 보장되지 않는 것으로 보인다(#68이 검증했던 위치와 다른 위치/다른 노드에서 재현됨 — #68의 재검증이 모든 경우를 커버하지 못한 것일 수 있음). **추가 관찰(같은 원인일 수 있어 함께 적어둠)**: 밤(`world_night` 스텝, `TimeData.is_day=false`)에는 좌상단 HUD 패널의 배경색 자체도 낮보다 어두워진다(픽셀 실측: 낮 `(23,49,27)` → 밤 `(16,27,22)`, 잔디가 어두워지는 것과 같은 방향/비슷한 비율) — CanvasModulate가 월드용으로만 걸려 있어야 정상이라면 UI CanvasLayer 색상은 안 바뀌어야 하는데 실제로는 바뀐다. UI가 CanvasModulate의 영향을 받는 것과 CanvasLayer 순서가 안 지켜지는 것이 같은 뿌리(예: 이 렌더러에서 `layer=10` UI가 실제로는 완전히 분리된 별도 컴포지팅 패스가 아닐 수 있음)일 가능성이 있으니 함께 조사할 것. 원인을 코드로 직접 조사해서 고치고, 고친 뒤에는 사슴/밭이 좌상단 HUD와 겹치는 상황을 다시 재현해(플레이어를 그 근처로 이동시키면 됨) 스크린샷으로 확인할 것.

- [x] #74 (완료 2026-09-03) [DESIGN] (버그 리포트, 사용자 직접 확인 + Claude 코드 확인) 총을 들고 정면/후면(남/북)을 볼 때와 좌우(동/서)를 볼 때 캐릭터 크기가 다르다. 원인 확인됨: `assets/sprites/character/gun/green_south_idle.png`/`green_north_idle.png`는 캔버스 68×68(다른 모든 도구·맨손과 동일)인데, `green_east_idle.png`/`green_west_idle.png`만 캔버스가 83×81로 더 크고 그 안의 캐릭터 자체도 더 크게 그려져 있다. 동/서를 다른 방향·다른 도구와 같은 68×68 캔버스·비슷한 캐릭터 크기로 다시 만든다(green 기준, `fire` 버전도 같은 캔버스로 맞출 것). 통과하면 blue/red 확장은 별도 항목으로 쪼갠다. (이동 중 사용 모션이 전혀 안 나가던 문제는 코드 버그였고 이미 고쳐졌다 — `world.gd` `_current_animation_name()` 참고, 이 항목은 순수 그림 크기만 다룬다.)
- [x] #75 (완료 2026-09-04, QA 좁은 범위 재검증 — 결함 0건, 재확인 루프 종료) [QA] "도구 관련 모션" 좁은 범위 재검증 (DESIGN.md "QA 관찰 항목" 형식을 그대로 따른다). **범위: 총/도끼/곡괭이낫/낚싯대 4개 전부.** 확인할 것: (1) 각 도구를 든 채 4방향 idle 크기가 서로 맞는지, 그리고 맨손/다른 도구와 비교해도 크기가 맞는지(#70/#71/#74가 다뤘던 것과 같은 종류의 문제가 남아있는지), (2) **이동하면서** 좌클릭해서 실제로 써봤을 때(총 발사/도끼 패기/곡괭이낫 채광·채집) 사용 모션이 화면에 정상적으로 나오는지 4방향 전부, (3) 이동 중 도구를 idle로만 들고 있을 때(사용 안 함) 맨손 걷기로 자연스럽게 전환되는지. 문제를 찾으면 각각 새 항목으로 등록하고(코드 문제면 [BUILD], 그림 문제면 [DESIGN]) 이 항목과 똑같은 내용의 "QA 재확인" 항목을 뒤에 추가한다 — 사용자가 "완전히 고쳐질 때까지 계속 반복해서 테스트하라"고 명시적으로 지시했으니, 문제가 하나라도 남아있으면 재확인 루프를 절대 끊지 말 것. 문제를 하나도 못 찾았을 때만 새 항목 없이 종료한다.

- [x] #79 (완료 2026-09-04) [DESIGN] (사용자 지시, 2026-09-04) 나무(logging) 정적 오브젝트를 파이썬 절차적 생성(Pillow)으로 만든다 — **AI 생성 도구(SpriteCook/PixelLab) 크레딧을 쓰지 않는다.** 근거: 캐릭터 리메이크(#76~#78, 취소됨 — 아래 "취소된 항목" 참고)는 유료 크레딧이 두 번이나 소진됐고 결과물도 비효율적이었어서, 사용자가 당분간 캐릭터 쪽 AI 생성은 보류하고 크레딧이 필요 없는 절차적 생성으로 전환하기로 함. 나무는 관절 없는 단순 정적 오브젝트라 DESIGN.md "그래픽 파이프라인"의 절차적 생성 허용 범위에 해당한다(잔디 텍스처 성공 사례, `game/assets/sprites/ground/grass_tile.png` 참고 — 같은 방식으로 수학적 도형 조합/그라디언트를 써서 만들 것). 몸통(직사각형/사다리꼴)+잎(원/타원 뭉치, 톤 2~3단계 음영)으로 스타듀밸리/코어키퍼 느낌의 나무 한 종류를 만들어 `assets/sprites/logging_point/tree.png`로 저장한다. 배경 투명(PNG 알파), 화면 기준 최소 96px 이상(나무는 "커야 할 이유가 있는" 오브젝트이므로 DESIGN.md 32px 최소 기준보다 크게— 캐릭터보다 확실히 크게 보이도록). 실제 게임 화면에 배치해서 스크린샷으로 확인하고, 인공적인 반복 패턴이나 어색한 경계선이 없는지 확대해서 직접 볼 것. 통과 못 하면 코드를 더 다듬지 말고 AI 생성으로 전환(DESIGN.md 규칙대로) — 단, 이번엔 시간 여유를 두고 시도해본다(대충 작업이어도 됨, 사용자가 명시적으로 "대충 해도 된다"고 지시함).
- [x] #80 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 도끼(axe)에게 실제 벌목 대상을 만들어준다 — 지금 `scenes/world/world.gd:62` 주석에 이미 "도끼는 아직 벌목 대상이 없고"라고 적혀 있는, 오래된 미완성 상태다. #79에서 만든 나무 그림을 쓰는 새 씬 `scenes/resource_point/logging_point.tscn`을 기존 `gathering_point.tscn`/`mining_point.tscn`과 같은 패턴(`resource_point.gd` 재사용, `required_tool="axe"`, `item_name="wood"`, `prompt_text="도끼를 들고 좌클릭: 벌목 (나무)"`)으로 만든다. `resource_point.gd`의 `_harvest()`가 지금 `world_ref.play_pickaxe_use(use_kind)`를 무조건 호출하는데, 도끼를 든 상태에서는 이미 `world.gd`의 전역 `_unhandled_input()`이 패기 모션을 별도로 재생하므로(197번째 줄 부근) `play_pickaxe_use`는 그냥 조용히 무시된다 — 실제로 게임을 실행해서 패기 모션이 정상적으로 나오는지 반드시 확인할 것(이론상 괜찮아 보여도 직접 확인 없이 통과시키지 말 것). `world.gd`의 `ITEM_LABELS`에 `"wood": "나무"`를 추가하고, `RESOURCE_POINT_COUNT`/스폰 로직(`GatheringPointScene`/`MiningPointScene`과 같은 자리) 옆에 `LoggingPointScene`을 추가해서 월드에 실제로 스폰되게 한다. 완성되면 실제로 도끼를 들고 나무에 다가가 좌클릭해서 나무 아이템이 드롭되고 인벤토리에 들어가는지, 패기 모션이 나오는지 스크린샷/직접 플레이로 확인한다.

- [x] #82 (완료 2026-09-04, 결함 0건) [QA] 전체 스윕: 메인 메뉴부터 모든 시스템을 실제로 플레이해보며 문제를 찾는다 (BUILD/DESIGN 완료 카운터가 5에 도달해 자동 등록됨 — #79/#80 등 최근 완료분, 상세는 STATUS.md 참고). `game/qa/full_sweep.gd`를 재사용할 수 있다 — 이번엔 새로 생긴 벌목(logging_point/나무/도끼) 흐름을 특히 확인할 것.
- [x] #81 (완료 2026-09-04, 통과 — 상세는 STATUS.md 참고) [DESIGN] (사용자 지시, 2026-09-04) 캐릭터도 AI 생성 없이 파이썬 절차적 생성으로 일단 찍어본다 — 실험 항목이라 **품질 기준을 낮춘다(도트가 엉성해도 됨, 사용자가 명시적으로 허용).** DESIGN.md "그래픽 파이프라인"의 2026-09-04 변경(유기체도 절차적 생성 허용, 완화된 합격 기준 — 실루엣/방향 구분 정도면 통과) 내용을 먼저 읽는다. 남자 기준 캐릭터 맨손 idle 4방향(`assets/sprites/character/male_{south,north,east,west}.png`)을 Pillow로 만든다 — 참고 이미지(`docs/design_reference/style_ref_*.png`)의 다부호 비율/음영 단계를 최대한 흉내 내보되, 똑같이 안 나와도 된다. 최소 기준: (1) 4방향이 서로 구별되는 자세/방향으로 보임, (2) 형체가 사람처럼 알아볼 수 있음(사각형 덩어리로만 안 보임), (3) 4방향 캔버스 크기가 서로 일관됨. 실제 게임에 장착해서(임시로 `male` variant 스폰 또는 QA 스크립트) 4방향 스크린샷으로 확인한다. 이 낮춘 기준으로도 형체를 알아볼 수 없는 수준이면(예: 그냥 색 블록), 코드를 더 다듬지 말고 STATUS.md에 결과와 스크린샷 경로를 기록한 뒤 **사람에게 보고하고 다음 지시를 기다린다** — AI 생성으로 스스로 되돌리지 않는다. 통과하면 커밋(여자/걷기/도구별 확장은 별도 항목으로 쪼갠다).

- [x] #83 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 아이템 카테고리 체계 도입 + `iron` 아이템 이름을 `iron_ore`로 정정. DESIGN.md "아이템 카테고리 / 제작(크래프팅) 테크" 절을 먼저 읽는다. `world.gd`의 `ITEM_LABELS`(현재 키→표시이름 평면 딕셔너리) 옆에 `ITEM_CATEGORIES`(아이템 키 → "원재료"/"가공물"/"완성품"/"곡물"/"육류"/"채소"/"가공식품"/"요리"/"도구" 등 카테고리 문자열) 딕셔너리를 새로 추가하고, 지금 존재하는 모든 아이템(`rice_seed`, `rice`, `iron`, `wood`, `captured_deer`, `gun`/`axe`/`pickaxe`/`fishing_rod`)을 분류해서 채운다. 그리고 `iron`이라는 아이템 키를 쓰는 모든 참조 지점(`world.gd`의 `ITEM_LABELS`, `scenes/resource_point/mining_point.tscn`의 `item_name`, `scenes/dropped_item/dropped_item.gd`의 `ITEM_ICONS` 등 — grep으로 전부 찾을 것)을 `iron_ore`로 통일 개명하고 표시 이름도 "철"에서 "철광석"으로 바꾼다(가공된 "철"은 이 항목에서 만들지 않는다 — 원재료 이름만 바로잡는 것). 세이브 파일 호환은 신경 쓰지 않아도 된다(테스트 단계). 이 항목은 새 아이템/오브젝트를 만들지 않고 분류 체계 마련 + 이름 정정까지만 한다. 게임을 실행해서 인벤토리 창에 "철광석"으로 정상 표시되고 광산에서 캐도 정상 동작하는지 확인한다.
- [x] #84 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 필드 채광 포인트에서 돌/철광석/유황광석이 확률적으로 섞여 나오게 만든다(돌이 가장 흔하고 철광석, 유황광석 순으로 희귀 — DESIGN.md "예시 자원" 절 참고, #83이 먼저 끝나서 `iron_ore` 이름 정정이 된 뒤 진행). 새 원재료 아이템 `stone`(돌), `sulfur_ore`(유황광석)를 `ITEM_LABELS`/`ITEM_CATEGORIES`(둘 다 "원재료")에 추가한다. 지금 `mining_point.tscn` 하나(`item_name="iron_ore"`)만 있는 것을, 같은 `resource_point.gd`를 재사용하는 `mining_point_stone.tscn`(`item_name="stone"`, `prompt_text`도 돌에 맞게 수정)과 `mining_point_sulfur.tscn`(`item_name="sulfur_ore"`)로 복제해서 만든다. `world.gd`의 `_spawn_resource_points()`(372번째 줄 부근)가 지금 `MiningPointScene` 하나만 고정 스폰하는 부분을, 세 씬 중 하나를 가중치 랜덤(돌 60%, 철광석 30%, 유황광석 10% 정도 — 정확한 수치는 재량)으로 골라 스폰하도록 바꾼다. 텍스처/그림은 이번 항목 범위가 아니다(기존 mining_point 그림을 그대로 재사용하거나 `modulate`로 색만 다르게 임시 처리 — 실제 그림은 후속 [DESIGN] 항목 #86에서 교체). 실행해서 돌/철광석/유황광석 포인트가 실제로 섞여서 스폰되는지, 각각 캐면 올바른 아이템이 드롭되는지 확인한다.
- [x] #85 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 사슴을 기본탄으로 사살하면 고기(육류 카테고리)가 드롭되도록 만든다. `scenes/deer/deer.gd`의 `_die()`(현재 `_dead = true; queue_free()`뿐이고 아무것도 드롭하지 않음, 221번째 줄 부근)에 `_capture()`(226번째 줄, `world_ref.spawn_dropped_item(CAPTURED_ITEM, 1, global_position)`)와 같은 패턴으로 `world_ref.spawn_dropped_item("meat", 1, global_position)`를 추가한다(`world_ref`가 null이 아닐 때만, `_capture()`와 동일 가드). 새 아이템 `meat`를 `ITEM_LABELS`(표시 이름 "고기")/`ITEM_CATEGORIES`(#83에서 만든 체계, "육류")에 추가한다. 텍스처/그림은 이번 항목 범위가 아니다(후속 [DESIGN] 항목 #86에서 만듦, 그때까지는 기본 아이콘 없이도 인벤토리에 텍스트로만 표시돼도 됨). 실제로 사슴을 기본탄으로 쏴 죽여서 고기가 드롭되고 인벤토리에 들어가는지, 마취탄으로 생포하는 기존 흐름은 그대로 동작하는지(회귀 없는지) 둘 다 확인한다.
- [x] #86 (완료 2026-09-04) [DESIGN] (사용자 지시, 2026-09-04) #84/#85에서 새로 생긴 원재료/식재료 아이템 3종(돌/유황광석/고기)의 아이템 아이콘과, 돌/유황광석 채광 포인트의 월드 그림을 파이썬 절차적 생성(Pillow)으로 만든다. **AI 생성 도구(SpriteCook/PixelLab) 크레딧을 쓰지 않는다** — #79 나무/#81 캐릭터와 같은 기법(도형 레이어링 + 그라디언트 + 안티에일리어싱, `/tmp/gen_*.py` 관례 참고)을 재사용한다. 돌은 회색 계열 각진 덩어리, 유황광석은 노란빛이 도는 결정형, 고기는 붉은 살코기 덩어리 느낌으로 — 참고 게임(스타듀밸리/코어키퍼)의 단순하고 알아보기 쉬운 아이템 아이콘 스타일을 참고한다. 각각 인벤토리 아이콘(작은 크기)과 돌/유황광석은 추가로 월드 배치용 그림(DESIGN.md "아이템/오브젝트 크기 표준"대로 화면에서 충분히 크게, 최소 32px 이상)을 만든다. 배경 투명(PNG 알파) 필수. `scenes/dropped_item/dropped_item.gd`의 `ITEM_ICONS`/`mining_point_stone.tscn`·`mining_point_sulfur.tscn`의 `texture_path`에 실제로 연결하고, 게임을 실행해서 스크린샷으로 확인한다.
- [x] #87 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 가공 라인의 첫 오브젝트 **가공대(Processing Table)**를 만든다. DESIGN.md "생산 라인 — 작업대 2계통" 절을 먼저 읽는다. `farm_plot`/`ranch_zone`과 같은 패턴(플레이어가 `INTERACT_RADIUS` 안에 들어가면 프롬프트 표시)으로 새 씬 `scenes/processing_table/processing_table.gd`(+`.tscn`)를 만들되, 여러 레시피 중 고를 수 있는 제작 UI가 필요하다(인벤토리 창처럼 새 `Control` 팝업, 레시피 목록을 보여주고 클릭하면 제작). 레시피는 데이터 목록(`const RECIPES := [{"inputs": {"wood": 2}, "output": "plank", "amount": 1}, ...]` 형태)으로 관리해서 나중에 추가하기 쉽게 한다. 이번 항목에서는 레시피 2개만 먼저 구현해서 파이프라인을 검증한다: **목재 2개 → 판자 1개**, **돌 2개 → 석재 1개**. 제작 버튼을 누르면 인벤토리에서 재료를 확인(부족하면 제작 불가 표시)하고 소모한 뒤 결과물을 지급한다(`InventoryData`의 기존 카운트 기반 API 참고). 새 가공물 아이템 `plank`(판자)/`stone_block`(석재)을 `ITEM_LABELS`/`ITEM_CATEGORIES`("가공물")에 추가한다. 가공대 자체의 월드 그림은 이번 항목 범위가 아니다(임시로 단색 사각형이나 기존 아이콘 재사용, 실제 그림은 후속 [DESIGN] 항목 #91). 월드에 가공대 1개를 배치해서(위치는 재량) 실제로 목재를 판자로, 돌을 석재로 만들 수 있는지 확인한다.
- [x] #88 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 가공 라인의 두 번째 오브젝트 **제련로(Smelting Furnace)**를 만든다. #87의 가공대와 같은 프레임워크(상호작용+레시피 목록 UI, 코드 재사용/공통화할 것)를 그대로 써서 새 씬 `scenes/smelting_furnace/smelting_furnace.gd`(+`.tscn`)를 만든다. DESIGN.md 규칙대로 화로는 가공대와 별개 오브젝트다. 레시피 2개: **철광석 2개 → 철 1개**, **목재 2개 → 숯 1개**. 새 가공물 아이템 `iron`(철, "가공물" — #83에서 `iron_ore`로 개명한 원재료와는 다른 별개 아이템이므로 이름 충돌 없음)/`charcoal`(숯)을 `ITEM_LABELS`/`ITEM_CATEGORIES`에 추가한다. 월드 그림은 범위 밖(후속 #91). 월드에 제련로 1개를 배치해서 철광석→철, 목재→숯 제작이 실제로 되는지 확인한다.
- [x] #89 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 화약/탄약 레시피를 #87의 가공대에 추가한다(새 오브젝트 아님, 기존 가공대의 `RECIPES` 목록만 확장). 레시피 2개: **유황광석 1개 + 숯 1개 → 화약 1개**, **화약 1개 + 철 1개 → 탄약 3개**(수량은 재량, 밸런스는 나중에 조정). 새 완성품 아이템 `gunpowder`(화약, "가공물")/`ammo`(탄약, "완성품")를 `ITEM_LABELS`/`ITEM_CATEGORIES`에 추가한다. **주의: 이번 항목은 탄약 아이템을 "만들 수 있게"만 하는 것이고, 총의 재장전(R키)이 실제로 이 탄약을 소비하도록 연동하는 건 범위 밖이다** — 지금 총은 그대로 무한 재장전으로 두고 건드리지 않는다(DESIGN.md에도 이미 명시됨, 별도 지시가 오면 그때 연동). 가공대에서 화약과 탄약이 실제로 만들어지는지만 확인한다.
- [x] #90 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 조리 라인 오브젝트 **조리대(Cooking Table)** + **조리용 화로(Cooking Stove)**를 만든다(#87/#88과 같은 상호작용+레시피 UI 프레임워크 재사용, 씬은 `scenes/cooking_table/`·`scenes/cooking_stove/`). DESIGN.md 규칙대로 이 화로는 제련로(#88)와 완전히 별개 오브젝트다(가공 라인과 조리 라인의 화로를 겸용하지 않음). 레시피 2개(조리용 화로에서): **벼 1개 → 밥 1개**, **고기 1개 → 익힌고기 1개**. 새 가공식품 아이템 `cooked_rice`(밥)/`cooked_meat`(익힌고기)를 `ITEM_LABELS`/`ITEM_CATEGORIES`("가공식품")에 추가한다. **조합 요리(밥+익힌고기+익힌채소→스테이크 같은 것)는 이번 범위가 아니다** — 채소 아이템이 아직 정해지지 않아서 만들 수 없다(채소 추가 뒤 별도 지시로 처리). 월드에 조리대+조리용 화로를 배치해서 벼→밥, 고기→익힌고기 조리가 실제로 되는지 확인한다.
- [x] #91 (완료 2026-09-04) [DESIGN] (사용자 지시, 2026-09-04) #87~#90에서 만든 4개 오브젝트(가공대/제련로/조리대/조리용 화로)의 월드 그림과, 새로 생긴 가공물/완성품/가공식품 아이템(판자/석재/철/숯/화약/탄약/밥/익힌고기) 아이콘을 파이썬 절차적 생성(Pillow)으로 일괄 제작한다. **AI 생성 도구 크레딧을 쓰지 않는다** — #79/#81/#86과 같은 기법을 재사용한다. 4개 오브젝트는 서로 다른 실루엣으로 구별되게(가공대=작업대 모양, 제련로/조리용 화로=화로 모양이되 재질/색으로 구분, 조리대=조리대 모양) 만들고, DESIGN.md 크기 표준(오브젝트는 화면에서 충분히 크게, 최소 32px 이상, 캐릭터와 비교했을 때 자연스러운 비율)을 지킨다. 아이템 아이콘은 작고 단순하게(참고 게임 스타일). 각 씬의 `texture_path`/`ITEM_ICONS`에 실제로 연결하고, 게임을 실행해서 4개 오브젝트가 배치된 화면과 인벤토리 아이콘들을 스크린샷으로 확인한다.

- [x] #92 (완료 2026-09-04, QA 전체 스윕 — 결함 2건 발견, #94~#95로 티켓 발행) [QA] 전체 스윕: 메인 메뉴부터 모든 시스템을 실제로 플레이해보며 문제를 찾는다 (BUILD/DESIGN 완료 카운터가 5에 도달해 자동 등록됨 — 바퀴 135(#81)=1 → 136(#83)=2 → 137(#84)=3 → 138(#85)=4 → 139(#86)=5). `game/qa/full_sweep.gd`를 재사용할 수 있다 — 이번 구간에서 바뀐 것들을 특히 확인할 것: 돌/철광석/유황광석 채광 포인트가 실제로 섞여 스폰되는지(#84), 사슴을 기본탄으로 사살하면 고기가 드롭되는지(#85), 새로 생긴 돌/유황광석/고기 아이템 아이콘과 돌/유황광석 채광 포인트 월드 그림이 실제 화면에서 서로 구별되고 크기가 적절한지(#86).
- [x] #93 (완료 2026-09-04, QA 전체 스윕 — 결함 0건) [QA] 전체 스윕: 메인 메뉴부터 모든 시스템을 실제로 플레이해보며 문제를 찾는다 (BUILD/DESIGN 완료 카운터가 다시 5에 도달해 자동 등록됨 — #86 이후 리셋되고 140(#87)=1 → 141(#88)=2 → 142(#89)=3 → 143(#90)=4 → 144(#91)=5). `game/qa/full_sweep.gd`를 재사용할 수 있다 — 이번 구간(#87~#91)에서 새로 생긴 가공 라인/조리 라인 전체를 특히 확인할 것: 가공대(#87, 목재→판자/돌→석재)·제련로(#88, 철광석→철/목재→숯)·화약·탄약 레시피(#89)·조리대+조리용 화로(#90, 벼→밥/고기→익힌고기)가 실제로 재료를 소모하고 결과물을 지급하는지, 그리고 #91에서 새로 만든 4개 오브젝트 월드 그림(가공대/제련로/조리대/조리용 화로)과 8개 아이템 아이콘(판자/석재/철/숯/화약/탄약/밥/익힌고기)이 화면에서 서로 겹치지 않고 구별되며 크기가 적절한지. **#92가 완료됐으므로 바로 진행 가능.**
- [x] #94 (완료 2026-09-04) [DESIGN] (2026-09-04, QA #92가 발견) `assets/sprites/items/meat.png`(고기 드롭 아이콘)가 DESIGN.md "아이템/오브젝트 크기 표준"의 최소 32px 기준에 미달한다. 실측(화면 스크린샷 `/tmp/qa86/wide.png` 기준 배경 잔디 제외 픽셀 bbox): 돌 31×34px, 유황광석 38×32px인데 **고기만 44×27px로 짧은 변(세로)이 27px밖에 안 됨**. 32×32 캔버스 자체는 맞지만 그 안에 그려진 타원형 고기 도형이 세로로 캔버스를 충분히 채우지 못하고 납작하게 그려져 있는 게 원인으로 보인다. 다른 원재료 아이콘(`stone.png`/`sulfur_ore.png`)처럼 짧은 변 기준 32px 이상이 되도록 다시 그리거나(더 둥글게, 세로 여백 축소) 캔버스 안에서 확대해서 재배치한다. 절차적 생성(Pillow) 방식을 그대로 재사용해도 되고(#86/#91과 같은 스타일), 완성 후 실제 게임에서 드롭시켜 스크린샷으로 다른 아이템과 크기를 나란히 비교해 32px 이상임을 직접 확인한다.
- [x] #95 (완료 2026-09-04) [DESIGN] (2026-09-04, QA #92가 발견) 유황광석 채광 포인트(`mining_point_sulfur.tscn`)의 월드 그림이 "노란빛이 도는 결정형 광물"이라는 원래 의도(#86 원문)와 다르게, 회색 돌 위에 노란 뾰족한 스파이크 다발이 얹힌 형태라 광물보다 불꽃/폭발 이펙트처럼 보인다(스크린샷 `/tmp/qa86/wide.png`/`close.png`에서 확인 — 돌/철광석과 실루엣 구별은 잘 되지만 형태 자체가 "캘 수 있는 광물"로 안 읽혀서 처음 보는 사람이 위험 이펙트로 오인할 소지가 있음). `assets/sprites/resource_point/`(또는 해당 텍스처 경로)의 유황광석 그림을 결정질(각진 크리스탈 덩어리, 노란빛이 도는 반투명한 느낌)로 다시 그린다 — 돌(회색 둥근 블롭)/철광석과는 계속 실루엣이 구별되게 하되, 불꽃처럼 보이지 않도록 스파이크/화염 형태는 피한다. 완성 후 돌/철광석/유황광석 세 채광 포인트를 한 화면에 나란히 놓고 스크린샷으로 직접 비교해 확인한다.

- [x] #96 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 저장 상자(Storage Chest) 시스템을 신설한다. 새 씬 `scenes/storage_chest/storage_chest.gd`(+`.tscn`)를 만든다 — `farm_plot`/`processing_table`과 같은 "근처에서 상호작용" 패턴으로 접근하면 상자 UI(새 `Control` 팝업, 인벤토리 창과 별개)가 열린다. 상자는 `InventoryData`와 별개의 자체 슬롯 배열(`{"item": String, "count": int}` 형태, 슬롯 수는 재량이나 최소 20칸 이상)을 가지며, **상자 슬롯의 수량은 플레이어 인벤토리의 `STACK_MAX`(99) 제한을 받지 않고 그보다 큰 수(예: 999)를 그대로 저장할 수 있다** — 상자에서 플레이어 인벤토리로 옮길 때만 `InventoryData.add_item()`을 거쳐 플레이어 쪽 99 스택 제한이 자연스럽게 적용된다(옮기는 수량은 재량 — 클릭 한 번에 최대 99개씩 옮기거나, 상자 슬롯 전체를 넘기되 인벤토리 쪽에서 자동으로 여러 스택으로 나뉘게 하거나). 상자 UI는 슬롯마다 아이템 이름+수량을 표시하고 클릭하면 플레이어 인벤토리로 이전을 시도한다(인벤토리에 공간이 없으면 실패 표시). 이 오브젝트는 테스트용으로만 쓰는 게 아니라 나중에 실제 게임에도 쓰일 저장 상자의 기반이 되므로, 임시방편처럼 만들지 말고 `processing_table`/`smelting_furnace` 수준으로 제대로 만든다. 오브젝트의 월드 그림은 이번 항목 범위가 아니다(임시로 단색 사각형이나 기존 아이콘 재사용, 실제 그림은 나중 [DESIGN] 항목). 상자를 하나 배치해서 아이템을 넣고 빼는 게 실제로 되는지 확인한다(넣기 기능은 #97에서 씀 — 최소한 이 항목에서는 코드로 미리 채워넣은 슬롯을 꺼내는 것까지 확인).
- [x] #97 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) **테스트 환경 편의 기능**: #96의 저장 상자를 플레이어 스폰 지점 근처에 하나 배치하고, 지금까지 만든 크래프팅 재료 아이템을 종류(카테고리)별로 999개씩 채워 넣는다. 채워 넣을 아이템 15종(카테고리 순서대로): **원재료** — `rice_seed`, `iron_ore`, `stone`, `sulfur_ore`, `wood`. **곡물** — `rice`. **육류** — `meat`. **가공물** — `plank`, `stone_block`, `iron`, `charcoal`, `gunpowder`. **완성품** — `ammo`. **가공식품** — `cooked_rice`, `cooked_meat`. (도구 `gun`/`axe`/`pickaxe`/`fishing_rod`는 이미 무료 지급되고, `captured_deer`는 생포 전용 아이템이라 이 재료 상자에서는 제외한다.) 코드에서 월드 생성 시(`world.gd`의 다른 스폰 로직들 옆) 이 상자를 스폰하고 각 슬롯에 위 아이템+999를 채워 넣는 초기화 함수를 추가한다. **이 상자는 정식 밸런스의 일부가 아니라 테스트/개발 편의용이라는 걸 코드 주석과 STATUS.md에 명시할 것** — 나중에 실제 게임 밸런스를 잡을 때 이 자동 스폰을 없애거나 디버그 전용으로 옮기는 정리가 필요할 수 있다는 점도 같이 적어둔다. 실제로 게임을 실행해서 상자를 열어 15종 아이템이 각각 999개 들어있는지, 인벤토리로 하나씩 옮겨서 가공대/제련로/조리대/조리용 화로 레시피를 실제로 테스트할 수 있는지 확인한다.

- [x] #98 (완료 2026-09-04) [BUILD] (버그 리포트, 사용자 직접 확인, 2026-09-04) 인벤토리에 공간이 없을 때 아이템이 통째로 사라지는 버그 2건을 고친다. **원인 확인됨**: `scripts/inventory_data.gd`의 `add_item(item_name, amount) -> void`가 슬롯이 모자라면 넣은 만큼만 조용히 넣고 나머지를 그냥 버리는데(반환값이 없어 호출부가 얼마나 실패했는지 알 방법이 없음), 두 호출부가 이걸 확인 없이 쓴다: (1) `scenes/dropped_item/dropped_item.gd`의 `_process()`가 플레이어가 닿기만 하면 인벤토리 상태와 무관하게 `add_item()` 호출 후 무조건 `queue_free()`해서, 인벤토리가 꽉 찬 상태로 주우면 아이템이 통째로 증발한다. (2) `scenes/world/world.gd`의 `_on_craft_pressed()`(940번째 줄 부근)가 재료를 먼저 소모한 뒤 `InventoryData.add_item(output, amount)`을 확인 없이 호출해서, 제작 시 인벤토리가 꽉 차 있으면 재료만 사라지고 결과물도 안 들어온다. **참고로 `scenes/storage_chest/storage_chest.gd`의 `try_transfer_to_player()`는 이미 `_inventory_has_room()`으로 미리 확인하고 공간이 없으면 아무것도 옮기지 않는 올바른 패턴을 쓰고 있다** — 이 패턴을 재사용/공통화한다. `InventoryData`에 공용 메서드(예: `has_room(item_name: String, amount: int) -> bool`, 또는 실제로 넣은 양을 반환하는 `add_item(item_name, amount) -> int`로 시그니처 변경)를 추가하고, `storage_chest.gd`의 `_inventory_has_room()` 로직을 여기로 옮겨 통합해도 된다(중복 제거, 선택). `dropped_item.gd`를 고쳐서: 주울 양이 인벤토리에 다 안 들어가면, **들어가는 만큼만 넣고 넣은 만큼만 `item_amount`에서 차감**하고(전부 못 들어가면 0개 처리, 아예 안 주움) 드롭 오브젝트는 남은 수량이 있는 한 사라지지 않게 한다(다음 프레임에 다시 시도). `_on_craft_pressed()`도 고쳐서: 재료를 소모하기 **전에** 결과물이 인벤토리에 들어갈 공간이 있는지 먼저 확인하고, 공간이 없으면 재료를 소모하지 않고 제작 자체를 실패 처리한다(버튼 옆에 "인벤토리 공간 부족" 같은 메시지를 잠깐 보여줘도 좋음, 필수는 아님). 실제로 인벤토리를 꽉 채운 상태에서 (a) 바닥 아이템을 밟아도 사라지지 않는지, (b) 제작을 시도해도 재료가 사라지지 않는지 확인한다.
- [x] #99 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 제작 방식을 "누르면 즉시 완성돼서 인벤토리에 바로 들어옴"에서 **"수량 지정 → 재료 선소모 → 타이머(1개당 5초) → 제작 공간에 결과물이 쌓임 → 플레이어가 직접 수령"** 방식으로 전면 개편한다. 가공대/제련로/조리대/조리용 화로 4개 전부에 적용한다(이미 `world.gd`의 `open_crafting_window()`/`_on_craft_pressed()`를 공통으로 쓰고 있으니 그 프레임워크를 고치면 4개 다 한 번에 적용됨). **주의: #98(인벤토리 꽉 찼을 때 아이템 소실 버그 수정)을 먼저 완료한 뒤 진행한다** — 이번 항목의 "수령" 단계가 그 안전한 패턴(공간 확인 후 이전, 부족하면 제작 공간에 그대로 둠)을 그대로 재사용해야 하기 때문이다. 구체 사양: (1) 제작 UI에 레시피별로 수량을 지정하는 입력(SpinBox 또는 +/- 버튼, 기본값 1)을 추가한다. (2) "제작 시작"을 누르면 지정한 수량 전체분의 재료가 있는지 확인하고, 있으면 **그 시점에 전량 한 번에 소모**한다(부족하면 시작 안 됨). (3) 그 뒤로 `CRAFT_SECONDS_PER_UNIT := 5.0`(1개당 5초 — **이 수치는 명시적으로 임시값이며 나중에 다 바뀔 예정이니 상수로 빼서 쉽게 찾을 수 있게 할 것**) 간격으로 결과물이 1개씩 생산되어, 인벤토리가 아니라 **그 제작대/화로 자체의 내부 출력 버퍼**(`{item, count}` 형태, 저장 상자와 비슷한 개념이지만 재사용 안 해도 됨 — 새로 만들어도 됨)에 쌓인다. 이 타이머는 `_process(delta)`로 실시간 진행되고, 플레이어가 UI를 닫거나 자리를 떠나도 계속 진행된다(다시 상호작용하면 진행 상황이 그대로 보임). (4) 제작 UI(또는 별도 "수령" 버튼)에 지금까지 쌓인 출력 버퍼 수량과 남은 시간/개수를 표시하고, 수령 버튼을 누르면 #98에서 만든 안전한 방식으로 플레이어 인벤토리에 옮긴다 — 공간이 부족하면 옮길 수 있는 만큼만 옮기고 나머지는 계속 제작대 출력 버퍼에 남아있는다(사라지지 않음). (5) 한 번에 한 배치만 진행 가능하면 충분하다(이미 배치가 진행 중이면 새 배치 시작 버튼을 비활성화 — 여러 배치 동시 진행/큐잉은 이번 범위 아님). (6) 나중에 NPC/자동화가 이 출력 버퍼에서 자동으로 상자에 옮기게 만들 계획이니, 출력 버퍼가 플레이어 UI 코드에만 강하게 묶여있지 않고 별도로 접근 가능한 구조면 좋다(엄격한 요구사항은 아님). UI 예쁘게 만드는 건 이번 범위가 아니다(기능만 되면 됨, 사용자가 명시적으로 "UI는 나중에" 언급함). 4개 제작대 중 최소 하나(가공대)로 수량 3개 정도 제작해서 5초 간격으로 하나씩 출력 버퍼에 쌓이는지, 수령이 정상 작동하는지, 인벤토리를 꽉 채운 채로 수령해도 아이템이 사라지지 않고 버퍼에 남는지 확인한다.

- [x] #100 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 나무벽/나무문/석제벽을 **아이템으로 제작할 수만 있게** 가공대에 레시피로 추가한다. **설치(월드에 실제로 벽/문을 배치해서 방을 짓는 것)는 이번 범위가 아니다 — 나중에 별도로 진행한다(사용자가 명시적으로 범위를 좁힘).** 이 항목은 #99(제작 방식 개편: 수량 지정+타이머+수동 수령)가 완료된 뒤 진행한다 — 새 레시피도 그 방식 그대로 적용되면 된다(새 오브젝트나 새 UI를 따로 만들지 않음, `processing_table.gd`의 `RECIPES`에 항목만 추가). 새 완성품 아이템 3종을 `ITEM_LABELS`/`ITEM_CATEGORIES`("완성품")에 추가한다: `wood_wall`(나무벽, 레시피 예: 판자 4개 → 1개), `wood_door`(나무문, 레시피 예: 판자 3개 → 1개), `stone_wall`(석제벽, 레시피 예: 석재 4개 → 1개). 정확한 재료 개수는 재량(밸런스는 나중에 조정, DESIGN.md에도 "수치는 나중에 다 바뀔 예정"이라고 이미 기록돼 있음). 텍스처/그림은 이번 항목 범위가 아니다(후속 [DESIGN] 항목에서 만듦, 그때까지 인벤토리에 텍스트로만 표시돼도 됨). 가공대에서 세 레시피 모두 실제로 재료를 소모하고 제작 공간에 쌓였다가 수령되는지 확인한다.
- [x] #101 (완료 2026-09-04) [DESIGN] (사용자 지시, 2026-09-04) #100에서 추가된 나무벽/나무문/석제벽 3종의 인벤토리 아이템 아이콘을 파이썬 절차적 생성(Pillow)으로 만든다. **AI 생성 도구 크레딧을 쓰지 않는다** — #79/#86/#91과 같은 기법(도형 레이어링+그라디언트+안티에일리어싱)을 재사용한다. 나무벽은 갈색 나무판 질감, 나무문은 나무벽과 같은 톤이되 문 형태(손잡이 등)로 구별, 석제벽은 회색 돌 블록 질감으로 — 서로 실루엣이 확실히 구별되게 만든다. DESIGN.md "아이템/오브젝트 크기 표준"(인벤토리 아이콘은 다른 아이템들과 크기가 맞게)을 지킨다. 배경 투명(PNG 알파) 필수. `scenes/dropped_item/dropped_item.gd`의 `ITEM_ICONS`에 연결하고, 실제 게임에서 인벤토리/제작 UI에 표시된 걸 스크린샷으로 확인한다.
- [x] #102 (완료 2026-09-04, QA 전체 스윕 — 결함 1건 발견, #112로 티켓 발행) [QA] 전체 스윕: 메인 메뉴부터 모든 시스템을 실제로 플레이해보며 문제를 찾는다. (BUILD/DESIGN 완료 카운터가 5에 도달해 자동 등록됨 — PROMPT_BUILD.md ③ 규칙. 직전 스윕은 #93, 그 이후 완료된 BUILD/DESIGN: #94/#95/#96/#97/#98.) 특히 이번 스윕에서 놓치지 말아야 할 것: (1) INBOX #98이 고친 "인벤토리 꽉 찼을 때 아이템 소실" 안전 패턴이 저장 상자 이전/드롭 줍기/제작 수령 등 실제 여러 경로에서 잘 유지되는지, (2) 저장 상자(#96/#97)·가공/조리 라인 전체가 여전히 정상 동작하는지. `game/qa/full_sweep.gd`를 재사용하되 저장/캐릭터 세이브(`{characters,inventory}.save`)를 먼저 지우고 처음부터 스윕할 것.
- [x] #112 (완료 2026-09-04) [BUILD] (버그 리포트, #102 QA 전체 스윕에서 발견) 저장 상자/가공대/제련로/조리대/조리용 화로 — 5개 상호작용 창을 열면 전부 플레이어 캐릭터(그리고 근처의 다른 월드 오브젝트, 예: 사슴·상자의 임시 ColorRect 그림)가 창 뒤가 아니라 **창 패널 위에 그대로(반투명 딤 처리도 안 된 채 또렷하게)** 겹쳐 그려진다. 재현: 어느 상자/작업대든 근처에서 좌클릭해 창을 열면(카메라는 항상 플레이어를 따라가 화면 중앙 근처에 있고, 창도 `CenterContainer`로 화면 중앙에 뜨므로 이 상태는 실제 플레이에서 거의 매번 발생한다), 플레이어 스프라이트가 창의 반투명 딤 오버레이(`Color(0,0,0,0.55)`)와 불투명해 보이는 `PanelContainer` 배경 위로 완전한 색상/디테일로 노출된다 — 딤 처리로 어두워지는 것도 아니고 패널에 가려 안 보이는 것도 아니라, 마치 UI보다 위 레이어에 그려지는 것처럼 보인다. `scenes/world/world.gd`의 `_build_crafting_window()`(847번째 줄 부근)와 `_build_storage_window()`(1049번째 줄 부근)가 둘 다 만든 창을 `ui_layer.add_child(window)`(`ui_layer`는 `layer=10`으로 이미 명시된 `UI` CanvasLayer, INBOX #68에서 이 값을 고정한 바로 그 문제와 같은 종류로 보임)에 붙이는데도 이 현상이 나온다 — INBOX #68/#75(바퀴 120/127) 때 조사했던 "이 프로젝트의 GL Compatibility 렌더러에서 CanvasLayer 순서가 문서와 다르게 동작할 수 있다"는 그 계열의 문제가 재발했거나, 아직 못 찾은 별도 원인(예: Player/AnimatedSprite2D 쪽의 z_index나 top_level 설정)일 수 있다 — 원인을 직접 조사해서 고친다. **증거**: 이번 스윕이 새로 커밋한 `game/qa/full_sweep.gd`의 `storage_chest_open`/`processing_table_open`/`smelting_furnace_open`/`cooking_table_open`/`cooking_stove_open` 5개 스텝 스크린샷 전부에서 재현됨 — 그리고 이미 커밋돼 있던 예전 스크린샷(`/tmp/qa96/02_window_open_filled.png`, INBOX #96/바퀴 149)에도 같은 현상이 이미 찍혀 있었다(그때는 아무도 지적하지 않고 넘어간 것으로 보인다). 고친 뒤 5개 창 전부(플레이어를 창 화면 좌표와 겹치게 배치한 상태)를 스크린샷으로 재확인할 것.

- [x] #103 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 필드 채광 포인트에 모래/구리광석을 추가한다(#84가 만든 돌/철광석/유황광석 3종 가중치 랜덤 스폰 목록을 5종으로 확장). 새 원재료 아이템 `sand`(모래)/`copper_ore`(구리광석)를 `ITEM_LABELS`/`ITEM_CATEGORIES`(둘 다 "원재료")에 추가한다. `mining_point_stone.tscn`/`mining_point_sulfur.tscn`(#84)과 같은 방식으로 `mining_point_sand.tscn`/`mining_point_copper.tscn`을 만들고, `world.gd`의 `_spawn_resource_points()`의 가중치 목록에 추가(비중은 재량 — 강철/전기 테크의 기반 재료라 유황광석과 비슷하거나 약간 흔한 정도를 제안). 텍스처는 범위 밖(후속 [DESIGN] #104, 그때까지 기존 광물 그림 재사용/modulate 임시 처리). 실행해서 5종 채광 포인트가 섞여 스폰되고 각각 올바른 아이템을 드롭하는지 확인한다.
- [x] #104 (완료 2026-09-04) [DESIGN] (사용자 지시, 2026-09-04) #103에서 추가된 모래/구리광석의 아이템 아이콘과 채광 포인트 월드 그림을 파이썬 절차적 생성(Pillow)으로 만든다. **AI 생성 도구 크레딧을 쓰지 않는다** — #86/#91과 같은 기법 재사용. 모래는 연한 베이지색 알갱이 질감, 구리광석은 붉은빛이 도는 광택 있는 결정형으로 — 기존 돌(회색)/철광석/유황광석(노란빛)과 실루엣·색이 확실히 구별되게 만든다. DESIGN.md 크기 표준을 지키고 배경 투명 필수. `dropped_item.gd`의 `ITEM_ICONS`/두 채광 포인트 씬의 `texture_path`에 연결하고 스크린샷으로 확인한다.
- [x] #105 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 제련로에 강철/유리/구리 레시피를 추가한다(#88이 만든 제련로의 `RECIPES`만 확장, 새 오브젝트 아님). **레시피 재료는 항상 1개만 쓴다**(DESIGN.md "생산 라인" 2026-09-04 규칙 — 재료 종류를 얕게 유지): `iron_ore`→`iron`은 이미 있음, 이번엔 **철 2개 → 강철(`steel`) 1개**, **모래(`sand`) 2개 → 유리(`glass`) 1개**, **구리광석(`copper_ore`) 2개 → 구리(`copper`) 1개**. 새 가공물 아이템 3종을 `ITEM_LABELS`/`ITEM_CATEGORIES`("가공물")에 추가한다. #99가 만든 수량 지정+타이머+수동 수령 방식이 그대로 적용된다(새로 만들 것 없음). 제련로에서 세 레시피 모두 실제로 동작하는지 확인한다.
- [x] #106 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 가공대에 부재료 5종 레시피를 추가한다(#87이 만든 가공대의 `RECIPES`만 확장). **레시피 재료는 항상 1개만 쓴다.** **못(`nail`)**: 철 1개 → 4개(대량 생산). **경첩(`hinge`)**: 철 2개 → 1개. **톱니바퀴(`gear`)**: 강철 2개 → 1개(#105 완료 후 진행). **구리선(`copper_wire`)**: 구리 1개 → 2개(#105 완료 후 진행). **유리병(`glass_bottle`)**: 유리 1개 → 1개(#105 완료 후 진행). 5종 모두 `ITEM_LABELS`/`ITEM_CATEGORIES`("가공물")에 추가한다. 이 5개는 그 자체로 최종 소비 아이템이 아니라 **다른 완성품에 한 개씩만 들어가는 공용 부재료**다(#108/#109에서 사용) — 이번 항목에서는 만들 수 있게만 하면 된다. 가공대에서 다섯 레시피 모두 실제로 동작하는지 확인한다.
- [x] #107 (완료 2026-09-04) [DESIGN] (사용자 지시, 2026-09-04) #105/#106에서 추가된 8개 가공물(강철/유리/구리/못/경첩/톱니바퀴/구리선/유리병)의 인벤토리 아이템 아이콘을 파이썬 절차적 생성(Pillow)으로 일괄 제작한다. **AI 생성 도구 크레딧을 쓰지 않는다** — #91/#86과 같은 기법 재사용. 각각 실제 소재 느낌이 드러나게(강철=은회색 광택, 유리=반투명 청록빛, 구리=붉은 금속광택, 못/경첩/톱니바퀴=작은 금속 부품 형태로 서로 구별, 구리선=꼬인 선 형태, 유리병=투명 병 모양) 만들고 서로 실루엣이 겹치지 않게 한다. DESIGN.md 크기 표준 지키고 배경 투명 필수. `dropped_item.gd`의 `ITEM_ICONS`에 연결하고 스크린샷으로 확인한다.
- [x] #108 (완료 2026-09-04) [BUILD] (사용자 지시, 2026-09-04) 강철 도구 3종을 가공대 레시피로 추가한다: `steel_pickaxe`(강철곡괭이낫, 강철 3개 → 1개), `steel_axe`(강철도끼, 강철 3개 → 1개), `steel_fishing_rod`(강철낚싯대, 강철 2개 → 1개). 셋 다 `ITEM_LABELS`/`ITEM_CATEGORIES`("도구")에 추가한다. #105(강철) 완료 후 진행. **이번 항목은 인벤토리에 아이템으로 만들 수 있게만 한다** — 손에 들었을 때 기존 곡괭이낫/도끼/낚싯대와 실제로 다른 성능(채집 속도/데미지 등)을 내는 건 범위 밖이다(아직 도구 스탯/등급 시스템 자체가 없음 — DESIGN.md "도구 등급" 절 참고, 나중에 별도로 처리). 가공대에서 세 레시피 모두 실제로 동작하는지 확인한다.
- [x] #109 [BUILD] (완료 2026-09-04) 강철 건축 완성품 + 창문 + 강철 갑옷을 가공대 레시피로 추가한다. **레시피 재료는 최대 2종까지만 쓴다**(DESIGN.md 규칙): `steel_wall`(강철벽, 강철 3개+못 1개 → 1개), `steel_door`(강철문, 강철 2개+경첩 1개 → 1개), `steel_chest`(강철 상자, 강철 4개+못 2개 → 1개 — #96 저장 상자와는 별개의 "완성품" 아이템일 뿐, 이번 항목에서 그 자체를 놓을 수 있는 새 저장 오브젝트로 만들지는 않는다, 나무벽/나무문처럼 아이템으로만), `window`(창문, 유리 2개+못 1개 → 1개), `steel_armor`(강철 갑옷, 강철 5개 → 1개 — 원단/가죽 안감을 쓰려 했으나 천의 원재료 출처가 아직 없어서[DESIGN.md 가죽/직물 테크 보류 참고] 이번엔 강철 단일 재료로 만든다, 나중에 원단 출처가 생기면 레시피를 조정해도 됨). 5종 모두 `ITEM_LABELS`/`ITEM_CATEGORIES`("완성품")에 추가한다. #105/#106 완료 후 진행(강철/못/경첩/유리 필요). 인벤토리/장비 슬롯에 정상적으로 들어가는지 확인하되, 강철 갑옷을 장비 슬롯에 장착했을 때 실제 방어 효과가 나는지는 범위 밖(장비 효과 시스템 자체가 아직 없음, DESIGN.md "장비 슬롯" 절 참고). 가공대에서 다섯 레시피 모두 실제로 동작하는지 확인한다.
- [x] #110 [BUILD] (완료 2026-09-04) (사용자 지시, 2026-09-04) 전기 계열 완성품 3종 + 사료 + 급수 장치를 가공대 레시피로 추가한다. **레시피 재료는 최대 2종까지만 쓴다**: `battery`(배터리, 구리 2개+유황광석 1개 → 1개), `lamp`(조명, 구리선 1개+유리 1개 → 1개), `generator`(발전기, 톱니바퀴 2개+구리선 3개 → 1개), `feed`(사료, 벼 3개 → 2개), `water_pump`(급수 장치, 구리선 1개+강철 1개 → 1개). `battery`/`lamp`/`generator`/`water_pump`는 `ITEM_CATEGORIES`("완성품"), `feed`는 "가공식품"으로 분류한다. #105/#106 완료 후 진행. **이번 항목은 아이템으로 만들 수 있게만 한다** — 조명이 실제로 어둠을 밝히거나, 발전기가 실제로 전력을 생산하거나, 배터리가 뭔가에 쓰이거나, 사료/급수 장치가 목장 동물에게 실제 효과를 주는 것은 전부 범위 밖이다(전기/기계 테크 자체가 아직 방향만 잡혀있고 구체 시스템이 없음 — DESIGN.md "새 테크 라인" 절 참고, 나중에 별도로 설계). 가공대에서 다섯 레시피 모두 실제로 동작하는지 확인한다.
- [x] #111 (완료 2026-09-04) [DESIGN] (사용자 지시, 2026-09-04) #108~#110에서 추가된 11개 완성품(강철곡괭이낫/강철도끼/강철낚싯대/강철벽/강철문/강철 상자/창문/강철 갑옷/배터리/조명/발전기)과 가공식품 1개(사료)의 인벤토리 아이템 아이콘을 파이썬 절차적 생성(Pillow)으로 일괄 제작한다(급수 장치는 #106에서 만든 구리선/강철 조합이라 별도 그림 없이 두 아이콘을 조합해서 보여줘도 되고, 새로 하나 만들어도 됨 — 재량). **AI 생성 도구 크레딧을 쓰지 않는다.** 강철 도구 3종은 기존 도구 아이콘과 같은 구도에 은회색 톤만 다르게, 강철벽/강철문/강철 상자/창문은 #101에서 만든 나무벽/나무문/석제벽과 나란히 놓아도 어색하지 않은 톤으로, 배터리/조명/발전기는 작은 기계 부품 느낌으로 서로 구별되게 만든다. DESIGN.md 크기 표준 지키고 배경 투명 필수. `dropped_item.gd`의 `ITEM_ICONS`에 연결하고 스크린샷으로 확인한다.
- [x] #113 (완료 2026-09-04, QA 전체 스윕 — 결함 1건 발견, #115로 티켓 발행) [QA] 전체 스윕: 메인 메뉴부터 모든 시스템을 실제로 플레이해보며 문제를 찾는다. `game/qa/full_sweep.gd`(바퀴 155가 저장 상자/가공대/제련로/조리대/조리용 화로 5개 창을 여는 스텝까지 추가해둔 버전)를 재사용하고, INBOX #103이 새로 추가한 모래/구리광석 채광 포인트(임시 modulate 색 처리 상태)도 스윕 경로에서 실제로 한 번 캐보는 스텝을 추가해서 확인한다.
- [x] #114 (완료 2026-09-04, QA 전체 스윕 — 결함 0건) [QA] (자동 등록, 2026-09-04, BUILD/DESIGN 5개 완료 트리거 — #104~#108 사이클, #113과는 별개) 전체 스윕: 메인 메뉴부터 모든 시스템을 실제로 플레이해보며 문제를 찾는다. `game/qa/full_sweep.gd`를 재사용할 수 있다 — 이번 구간(#104~#108)에서 새로 생긴 것들을 특히 확인할 것: 모래/구리광석 채광 포인트 전용 그림(#104), 제련로의 강철/유리/구리 레시피(#105), 가공대의 못/경첩/톱니바퀴/구리선/유리병 부재료 레시피(#106)와 8개 신규 아이템 아이콘(#107), 가공대의 강철 도구 3종(강철곡괭이낫/강철도끼/강철낚싯대) 레시피(#108, 아이콘은 #111에서 아직 추가 전이라 인벤토리 텍스트로만 확인됨 — 정상).
- [x] #115 (완료 2026-09-04) [BUILD] (버그 리포트, #113 QA 전체 스윕에서 발견) 채집/채광/벌목 포인트(`resource_point.gd`)가 서로 겹칠 만큼 가까이 스폰될 수 있고, 그 상태에서 좌클릭 한 번이 겹친 포인트 전부를 동시에 채집/채광해버린다. **원인**: `world.gd`의 `_spawn_one_resource_point()`(574번째 줄 부근)가 새 포인트 위치를 정할 때 플레이어 스폰 위치와의 최소 거리(`RESOURCE_MIN_DISTANCE_FROM_PLAYER`)만 확인하고, **이미 배치된 다른 포인트와의 최소 거리는 전혀 확인하지 않는다** — 그래서 두 포인트가 서로의 `INTERACT_RADIUS`(70) 합보다 가깝게(실측 40~140 유닛 간격) 스폰되는 일이 실제로 자주 일어난다(필드에 흩뿌려지는 포인트 15개짜리 새 게임을 5번 반쳐돌려본 결과 3번에서 겹치는 쌍이 나왔다 — 우연이 아니라 흔한 케이스). 겹친 상태에서 플레이어가 둘 다의 `INTERACT_RADIUS` 안에 서면 두 `Prompt` 라벨이 같은 화면 위치에 겹쳐 그려져 텍스트가 뒤섞여 읽을 수 없게 되고(스크린샷으로 확인 — 예: "채광 (모래)"와 "채집 (벼 씨앗)"이 겹쳐 알아볼 수 없는 글자처럼 보임), 더 중요하게는 `resource_point.gd`의 `_unhandled_input()`이 어느 쪽도 `get_viewport().set_input_as_handled()`를 호출하지 않아서 좌클릭 한 번이 두 포인트의 `_unhandled_input()`에 전부 전달되어 **양쪽 다 동시에 채집/채광되는 것을 직접 재현해서 확인했다**(디버그 스크립트로 모래/구리광석 포인트를 40유닛 간격에 배치하고 좌클릭 이벤트 1회를 시뮬레이션 — 두 포인트 모두 `sprite.visible=false`+쿨다운 시작+드롭 아이템 생성이 동시에 일어남). 두 가지를 고친다: (1) `_spawn_one_resource_point()`가 새 위치를 고를 때 이미 스폰된 다른 리소스 포인트들과도 최소 거리(예: 두 포인트의 `INTERACT_RADIUS` 합보다 넉넉히 큰 값)를 확인하도록 재시도 루프에 조건을 추가한다. (2) 그래도 혹시 겹치는 경우를 대비해 `resource_point.gd`의 `_unhandled_input()`이 성공적으로 채집/채광을 실행했을 때 `get_viewport().set_input_as_handled()`를 호출해서 한 번의 클릭이 두 포인트를 동시에 채집하지 못하게 막는다(어느 게 처리될지는 씬 트리 순서에 맡겨도 됨 — 최소한 "한 번에 하나만" 규칙은 지켜야 함). 고친 뒤 실제로 두 포인트를 가깝게 배치해서(또는 여러 번 새 게임을 돌려 자연 발생을 재현해서) 좌클릭 한 번에 하나만 채집되는지, 프롬프트 텍스트가 겹치지 않는지 스크린샷/로그로 확인한다.

- [x] #116 (완료 2026-09-05) [BUILD] (사용자 지시, 2026-09-05) 테스트용 저장 상자(#96/#97)를 **용량 무제한**으로 바꾼다. 지금 `storage_chest.gd`는 슬롯 하나의 수량은 99 제한 없이 크게 저장할 수 있지만(#96 원문), **슬롯 개수 자체는 고정된 크기**(#96 완료 당시 "최소 20칸 이상"으로 만들어짐)라 아이템 종류가 계속 늘어나는 지금(현재 약 40종) 한 종류당 한 슬롯씩 다 채우기엔 부족할 수 있다. `storage_chest.gd`에 `unlimited: bool`(또는 유사한) 옵션을 추가해서, 이 옵션이 켜진 인스턴스는 **슬롯 배열이 아이템 종류 수만큼 자동으로 늘어나게**(고정 크기 배열 대신 필요할 때마다 새 슬롯을 추가) 만든다. 이 옵션은 **테스트용 상자에만** 켜고, 나중에 실제 게임 플레이용 상자(플레이어가 짓는 진짜 창고 등)를 만들 때는 끈 채로(고정 슬롯 수) 쓸 수 있게 일반 저장 상자 코드 자체는 그대로 재사용 가능해야 한다(옵션으로 분기, 별도 스크립트로 복제하지 않음). 상자 UI가 슬롯이 많아져도(현재 40개 이상, 앞으로 계속 늘어남) 스크롤 등으로 다 보이게 되는지 확인한다. 테스트 상자를 열어서 실제로 새 아이템 종류를 계속 추가해도(코드로 시뮬레이션해서) 슬롯이 부족해서 못 들어가는 일이 없는지 확인한다.
- [x] #117 (완료 2026-09-05) [BUILD] (사용자 지시, 2026-09-05) #116 완료 후, INBOX #97 이후 새로 추가된 아이템 26종을 테스트용 저장 상자에 999개씩(또는 #116으로 무제한이 됐으니 더 큰 수, 예: 9999개도 가능 — 재량) 채워 넣는다. 채울 아이템: `wood_wall`, `wood_door`, `stone_wall`, `sand`, `copper_ore`, `steel`, `glass`, `copper`, `nail`, `hinge`, `gear`, `copper_wire`, `glass_bottle`, `steel_pickaxe`, `steel_axe`, `steel_fishing_rod`, `steel_wall`, `steel_door`, `steel_chest`, `window`, `steel_armor`, `battery`, `lamp`, `generator`, `feed`, `water_pump`. (기존 무료 지급 도구 `gun`/`axe`/`pickaxe`/`fishing_rod`와 생포 전용 `captured_deer`는 #97과 같은 이유로 계속 제외 — 단 강철 도구 3종은 무료 지급이 아니라 제작 결과물이므로 포함한다.) **이번 항목을 끝으로, 이제부터는 새 아이템이 INBOX 항목으로 추가될 때마다 그 항목을 처리하는 바퀴가 직접 테스트 상자에도 채워 넣어야 한다** — 이 규칙은 `loop/PROMPT_BUILD.md`/`loop/PROMPT_DESIGN.md`에 이미 상시 규칙으로 추가돼 있으니(아래 참고) 매번 다시 지시받을 필요 없이 자동으로 지켜져야 한다. 실행해서 상자를 열어 26종이 전부 채워져 있는지 확인한다.

- [x] #118 (완료 2026-09-05, QA 전체 스윕 — 결함 0건) [QA] 전체 스윕: 메인 메뉴부터 모든 시스템을 실제로 플레이해보며 문제를 찾는다 (BUILD/DESIGN 완료 카운터가 5에 도달해 자동 등록됨 — 직전 스윕 #113 이후 완료된 BUILD/DESIGN: #115/#116, 이번 #116으로 5 도달). 이번 스윕에서 특히 확인할 것: 테스트용 저장 상자가 용량 무제한(#116)으로 바뀐 뒤에도 기존 상호작용(슬롯 클릭→인벤토리 이전, 인벤토리 공간 부족 시 실패 메시지 등)이 그대로 동작하는지, 슬롯이 많아진 상태에서 상자 UI 스크롤이 실제로 잘 보이는지.

- [x] #119 [BUILD] (완료 2026-09-05) 건축물 격자 배치 시스템의 기반을 만든다 — **나무벽(`wood_wall`) 하나로만 검증**한다(다른 벽/문 종류는 #121에서 확장). DESIGN.md "건축/방(Room) 시스템" 절(2026-09-05 격자 확정)을 먼저 읽는다. `world.gd`에 `BUILD_GRID_SIZE := 64.0`(기존 잔디 타일 크기와 맞춤) 상수를 추가하고, 월드 좌표를 격자 중심 좌표로 스냅하는 유틸리티 함수를 만든다. 플레이어가 핫바에서 `wood_wall`을 손에 들면 "배치 모드"로 들어간다: 마우스 커서 아래 가장 가까운 격자 칸에 반투명 미리보기(고스트) 스프라이트를 표시하고(`get_global_mouse_position()` 사용, 기존 조준 로직과 같은 패턴), **좌클릭하면 그 칸에 실제로 설치**(인벤토리에서 `wood_wall` 1개 소모, `StaticBody2D`+`CollisionShape2D`로 실제 충돌체를 만들어 플레이어가 통과 못 하게 함 — `ranch_zone.gd`의 담장 생성 패턴 참고), **우클릭 또는 도구를 바꾸면 배치 모드를 취소**한다. 이미 다른 건축물/오브젝트가 있는 칸에는 설치할 수 없게(겹침 검사) 막는다. 설치된 벽의 위치는 나중에(#122) 방 감지가 참조할 수 있도록 격자 좌표 기준으로 추적 가능한 자료구조(예: `Dictionary[Vector2i, Node]`)에 기록해둔다. 벽 그림은 이미 있는 아이템 아이콘(#101)을 재사용해도 되고 확대해서 써도 된다(새 그림 필요 없음). 실제로 나무벽 여러 개를 이어붙여서 플레이어가 못 지나가는지, 인벤토리 개수가 정확히 줄어드는지, 이미 벽이 있는 칸에 다시 설치가 안 되는지 확인한다.
- [x] #120 (완료 2026-09-05) [BUILD] (사용자 지시, 2026-09-05) #119의 격자 배치 시스템을 나무문(`wood_door`)에도 적용하되, 문은 벽과 달리 **여닫을 수 있어야 한다**. 설치는 #119와 같은 방식(배치 모드+격자 스냅+좌클릭 확정). 설치된 문에 플레이어가 가까이 다가가서 좌클릭(도구가 없거나 배치 모드가 아닐 때, 기존 `resource_point.gd` 스타일의 "근처에서 좌클릭" 패턴)하면 **열림/닫힘 상태가 토글**된다: 닫힘 = 충돌체 활성화(못 지나감, 배치 직후 기본 상태), 열림 = 충돌체 비활성화(지나갈 수 있음) + 시각적으로 구별되게(예: 투명도를 낮추거나 옆으로 살짝 이동한 것처럼 보이게 — 새 그림을 만들 필요는 없음, 기존 스프라이트를 변형해서 표현). **중요(DESIGN.md 2026-09-05 규칙): 문은 열려있든 닫혀있든 "설치돼 있다"는 사실만으로 방 경계 역할을 한다** — 이번 항목은 그 경계 판정(방 감지) 자체를 구현하지 않지만(#122 몫), 나중에 #122가 참조할 때 열림/닫힘 상태와 무관하게 "이 칸에 문이 있다"는 사실만 보면 되도록 자료구조를 설계해둔다. 실제로 문을 설치하고 열고 닫으면서 통과 가능 여부가 바뀌는지 확인한다.
- [x] #121 [BUILD] (완료 2026-09-05) (사용자 지시, 2026-09-05) #119/#120의 격자 배치 시스템을 나머지 벽/문 종류에 연결한다: `stone_wall`(석제벽), `steel_wall`(강철벽), `steel_door`(강철문), `window`(창문). 새 코드를 만들지 않고 #119/#120이 만든 프레임워크에 아이템 키만 추가하는 방식으로 한다(벽류는 #119 방식, 문류는 #120 방식 — `window`는 벽처럼 막히는 쪽으로 취급하되 나중에 "안이 보이는 벽" 취급이 필요해지면 그때 다시 다룬다, 지금은 막히기만 하면 됨). 5종(나무벽/나무문 포함 총 7종) 전부 실제로 설치해보고 충돌/문 여닫기가 정상 동작하는지 확인한다.
- [x] #122 (완료 2026-09-05) [BUILD] (사용자 지시, 2026-09-05) 방(Room) 감지 시스템을 만든다. DESIGN.md "건축/방(Room) 시스템" 절의 2026-09-05 확정 내용(방 종류별 규칙표, 잡실 판정)을 반드시 먼저 읽는다. #119~#121이 추적해둔 격자 점유 정보(벽/문 위치)를 바탕으로, 벽/문이 새로 설치되거나 제거될 때마다(매 프레임이 아니라 변경 시점에만) **각 열린 격자 칸에서 flood-fill(BFS)을 돌려서 "막힌(둘러싸인) 공간"을 찾는다** — 탐색 칸 수에 상한(예: 400칸)을 두고, 상한 안에서 탐색이 끝나면(더 못 퍼져나가면) 그 영역을 방으로, 상한을 넘기면(바깥의 열린 들판으로 새는 것으로 판단) 방 아님으로 처리한다. 방으로 인식된 영역마다 그 안에 있는 "핵심 오브젝트"(가공대/제련로/조리대/조리용 화로/연구대/farm_plot/ranch_zone — 연구대는 아직 게임에 없으므로 있으면 처리, 없으면 건너뜀)를 스캔해서, DESIGN.md 규칙표대로 카테고리를 판정한다: 카테고리가 정확히 1종류고 요구 수량(주방만 조리대+조리용 화로 둘 다 필요, 나머지는 1개 이상)을 채우면 그 이름, 0종류거나 2종류 이상 섞이면 "잡실". 저장 상자는 카테고리 판정에서 제외(중립)한다. 각 방에 고유 ID를 부여하고, 그 방에 속한 오브젝트(제작대류/상자)가 자기 방 ID를 조회할 수 있게 한다(#123이 이걸 씀). 화면에 방 이름을 표시할 필요는 없다(이번 항목 범위 아님, 필요하면 나중에 UI로). 실제로 벽 4개로 작은 공간을 완전히 막고 가공대를 넣으면 "제작소"로 판정되는지, 거기에 조리대를 추가로 넣으면 "잡실"로 바뀌는지, 벽 하나를 없애 트인 공간으로 만들면 방 자체가 인식 안 되는지 로그나 디버그 출력으로 확인한다.
- [x] #123 (완료 2026-09-05) [BUILD] (사용자 지시, 2026-09-05) 방-상자 자동 연동을 만든다. #122 완료 후 진행. DESIGN.md "건축/방(Room) 시스템" 절의 "방-상자 자동 연동" 항목을 먼저 읽는다. 제작대류(가공대/제련로/조리대/조리용 화로)가 재료 보유량을 확인/소모할 때, **자신이 속한 방(잡실이 아닌, #122가 판정한 정상 방)과 같은 방 ID를 가진 저장 상자**가 있으면 그 상자의 내용물도 재료로 함께 인식해서 쓸 수 있게 한다(플레이어 인벤토리만 보던 지금의 `InventoryData.get_count()`/`remove_item()` 호출부를 확장 — 상자 쪽 API는 `storage_chest.gd`에 이미 있는 슬롯 조회/차감 로직을 재사용하거나 필요한 만큼 새 메서드를 추가한다). 방 ID가 다르거나 잡실이면(또는 애초에 방이 없으면) 지금처럼 플레이어 인벤토리만 본다(기존 동작 그대로 유지, 회귀 없어야 함). 실제로 방(예: 제작소로 인식된 방) 안에 상자를 놓고 재료를 상자에만 넣은 채로, 플레이어 인벤토리가 비어 있어도 가공대에서 제작이 되는지 확인한다. 그리고 방 밖(잡실 포함)에서는 상자 재료가 자동으로 안 쓰이고 여전히 손으로 옮겨야 하는지도 함께 확인한다(회귀 검사).
- [x] #124 (완료 2026-09-05, QA 전체 스윕 — 결함 0건) [QA] 전체 스윕: 메인 메뉴부터 모든 시스템을 실제로 플레이해보며 문제를 찾는다. BUILD/DESIGN 항목 5개(#119~#123) 완료로 자동 등록됨(loop/PROMPT_BUILD.md ③ 규칙). 특히 확인할 것: 격자 배치 시스템 전체(#119~#121)와 방 감지(#122)가 실제 플레이에서 자연스럽게 동작하는지(벽 4개로 작은 방을 지어보고 판정이 맞는지 등), 방-상자 자동 연동(#123)이 실제로 재료 없이도 제작이 되는지.
- [x] #125 (완료 2026-09-05) [BUILD] (버그 리포트, 사용자 지시 2026-09-05, 스크린샷 첨부됨) 건축 격자가 처음부터 너무 크게 잡혀 있어서 벽을 설치하면 캐릭터에 비해 지나치게 커 보인다(사용자가 실제로 벽을 설치해본 스크린샷 기준). 원인으로 보이는 것: `game/scenes/world/world.gd:97`의 `const BUILD_GRID_SIZE := 64.0`. 이 값을 지금의 1/4 수준(또는 실제로 스크린샷으로 확인했을 때 캐릭터 대비 자연스러운 비율이 나오는 값)으로 줄이거나, 필요하면 격자 체계 전체를 다시 조정한다. `BUILD_GRID_SIZE`를 참조하는 다른 곳(고스트/벽/문 스프라이트 스케일링, `door.gd`의 `_grid_size` 등)도 전부 같이 맞춘다. 방 감지(#122)의 flood-fill 탐색 상한(400칸)은 격자가 작아지면 같은 물리적 면적이 더 많은 칸으로 나뉘게 되므로, 기존 방 크기 기준으로 상한값을 같이 재조정할 것(DESIGN.md "건축/방(Room) 시스템" 절 참고). 기존에 설치된 벽/문 위치 데이터와의 호환은 신경쓰지 않아도 된다(정식 세이브/로드 대상 아님). DESIGN.md "아이템/오브젝트 크기 표준"(최소 32px)은 지키되 지금처럼 과도하게 크지 않게. 벽 여러 개를 실제로 설치해서 스크린샷으로 비율을 확인한다.
- [x] #126 [BUILD] (완료 2026-09-05) (사용자 지시 2026-09-05) 방(Room) 오버레이 UI를 만든다. 화면 오른쪽 위에 툴바(토글 버튼 하나)를 추가하고, 누르면 "방 오버레이" 모드가 켜지고 다시 누르면 꺼진다(참고 레퍼런스: 산소미포함(Oxygen Not Included)의 방 종류 오버레이). 오버레이가 켜진 동안 화면에 보이는 격자 칸(#125에서 재조정된 `BUILD_GRID_SIZE` 기준) 위에 반투명 색을 겹쳐 그린다: `world.gd:1029`의 `get_room_category(room_id)`가 정상 방으로 판정한 칸은 방 종류(주방/제작소/대장간/연구소/농장/목장)별로 서로 다른 색, "잡실"이거나 방 자체가 없는 칸(벽 밖 야외 포함)은 회색으로 칠한다. **색은 완전 불투명이 아니라 알파값을 낮춰서 뒤의 바닥/오브젝트가 비쳐 보이게 한다**(사용자가 참고로 든 산소미포함 스타일 그대로). 방 배치가 바뀌면(#122가 방 감지를 다시 돌리는 시점) 오버레이도 같이 갱신되면 되고, 매 프레임 갱신할 필요는 없다. 이번 항목 범위는 토글 버튼 + 색칠 로직까지고, 방 이름 텍스트 표시는 범위 밖(기존에도 미완으로 남아있던 항목, 이번에 같이 안 해도 됨).
- [x] #127 [BUILD] (완료 2026-09-05, 버그 리포트/UI 정리, 사용자 지시 2026-09-05) 왼쪽 위 HUD를 정리한다. 1) `world.gd:291`의 `AmmoPanel`(`$UI/HUD/AmmoPanel/AmmoLabel`)은 지금 상시 표시되는데, **플레이어가 총을 손에 들고 있을 때만 보이고 다른 도구를 들거나 빈손이면 숨긴다**(총을 다시 들면 다시 보이게). 2) `world.gd:292`의 `InventoryPanel`(`$UI/HUD/InventoryPanel/InventoryLabel`, 상시 요약 표시)은 **완전히 제거한다** — 플레이어는 이미 E키로 여는 인벤토리 창(#21)이 있으니 상시 HUD 요약은 불필요하다는 사용자 판단. 인벤토리 패널을 지운 뒤 남은 HUD 요소(TimePanel/NetPanel/HotbarBar 등)의 레이아웃이 깨지지 않는지(빈 공간이 어색하게 남거나 겹치지 않는지) 스크린샷으로 확인한다.
- [x] #128 [BUILD] (완료 2026-09-05) (사용자 지시 2026-09-05) "건설 해제" 모드를 만든다. **X키**를 누르면 건설 해제 모드가 켜지고, 다시 누르면 꺼진다(배치 모드처럼 우클릭이 아니라 X 토글로 켜고 끔 — 배치 모드와 건설 해제 모드는 동시에 켜질 수 없으니, 한쪽이 켜질 때 다른 쪽은 자동으로 꺼지게 한다). 건설 해제 모드가 켜져 있는 동안: 1) 기존 배치 고스트와 같은 방식으로 마우스 아래 가장 가까운 격자 칸을 계산하고, **그 칸에 철거 가능한 설치물(이번 항목 범위: `_grid_occupancy`에 들어있는 벽/문/창문류 전부)이 있으면 그 오브젝트를 반투명 빨간색으로 덧칠**해서 "이걸 클릭하면 삭제된다"는 걸 플레이어가 미리 알 수 있게 한다(빈 칸이면 아무 표시 없음). 2) 그 상태에서 좌클릭하면 해당 오브젝트가 철거되어 대응하는 아이템(예: `wood_wall`)이 인벤토리에 1개 들어온다(가득 차 있으면 #24의 바닥 드롭 패턴 재사용, 100% 환급 — 손실 없음). `_grid_occupancy`(`world.gd:125`)에서 칸을 제거하고 노드를 `queue_free()`하며, 방 감지 재계산(#122, `world.gd:922` 근처)을 설치 때와 동일하게 트리거한다. 문은 열려있든 닫혀있든 철거 가능해야 한다. 건설 해제 모드가 꺼져 있을 때는 기존 동작(도구별 좌클릭 등)이 전혀 변하지 않아야 한다(회귀 없음). 실제로 X로 모드를 켜고 벽 위에 마우스를 올려 빨간색이 보이는지, 클릭해서 철거 후 인벤토리 개수가 원래대로 돌아오는지, 문 철거도 동일하게 동작하는지, 철거 후 방 감지가 올바르게 갱신되는지(방이었던 곳이 벽 하나 철거로 더 이상 방으로 인식 안 되는지), X를 다시 눌러 모드를 끄면 빨간 하이라이트가 사라지고 평소처럼 좌클릭이 도구 동작을 하는지 확인한다.
- [x] #129 [BUILD] (완료 2026-09-05) (버그 리포트, 사용자 지시 2026-09-05) 건축 배치 시 겹침 검사가 벽/문끼리만 되고 다른 오브젝트와는 안 되는 버그를 고친다. 지금 배치 확정 로직(`world.gd:848` 근처 `_grid_occupancy.has(cell)` 검사)은 `_grid_occupancy`(벽/문 전용 자료구조)만 확인해서, 같은 칸에 이미 `farm_plot`/`resource_point`(채집·채광 포인트)/`ranch_zone`/`storage_chest`/제작대류(가공대/제련로/조리대/조리용 화로)가 있어도, 심지어 **플레이어가 서 있는 칸에도** 벽/문을 설치할 수 있다(사용자가 실제로 겹쳐 설치되는 것을 확인함). 배치 확정 검사를 확장해서, 벽/문뿐 아니라 그 칸에 있는 다른 모든 점유 오브젝트(위에 나열한 것들 + 플레이어의 현재 격자 칸)와 겹치면 설치를 막는다. 막힐 때는 기존 고스트 미리보기의 빨간색 표시(`_build_ghost.modulate`, `world.gd:829`)를 그대로 재사용해서 사용자에게 알린다. 다른 오브젝트들의 격자 칸 판정에는 #125에서 재조정될 `BUILD_GRID_SIZE`를 기준으로 좌표를 스냅해서 비교한다(오브젝트 자체가 격자에 정확히 맞춰 배치돼 있지 않을 수 있으니, 오브젝트가 걸치는 모든 칸을 겹침으로 처리). 실제로 채집 포인트/상자 위에, 그리고 플레이어가 서 있는 칸에 벽 설치를 시도해서 막히는지 확인한다.
- [x] #130 (완료 2026-09-05) [BUILD] (사용자 지시 2026-09-05, #128 이후 진행) #128의 건설 해제 모드(X 토글 + 빨간 하이라이트 + 좌클릭 철거)를 상태를 가진 다른 설치물까지 확장한다: `storage_chest`(상자), 제작대류(가공대/제련로/조리대/조리용 화로), `farm_plot`(밭), `ranch_zone`(목장). 이것들은 벽/문과 달리 "안에 뭔가 든 상태"가 있을 수 있으므로 각각 규칙을 지킨다 — **상자**: 슬롯이 전부 비어있을 때만 철거 가능, 내용물이 하나라도 있으면 철거를 막는다(플레이어가 먼저 비워야 함). **제작대류**: 현재 진행 중인 제작 배치가 없을 때만 철거 가능(`is_batch_active()` 확인), 배치 진행 중이면 철거를 막는다. **목장**: 안에 동물이 있으면 철거를 막는다(동물을 먼저 빼야 함). **밭**: 언제든 철거 가능하고, 심어진 작물이 자라는 중이거나 수확 가능한 상태여도 그냥 함께 사라진다(작물을 별도로 되돌려주지 않음 — 의도된 단순화). 철거가 막힌 경우에도 빨간 하이라이트 자체는 그대로 보여주되(어떤 칸인지는 알 수 있게), 좌클릭해도 아무 일도 일어나지 않게 한다(막힌 이유를 화면에 텍스트로 안내할 필요는 없음, 이번 항목 범위 밖). 철거 성공 시 해당 아이템이 인벤토리로 돌아오는 것은 #128과 동일(100% 환급). 제작대류/상자가 철거되어 방 안의 핵심 오브젝트 구성이 바뀌면 방 카테고리 판정(#122)도 다시 계산되게 한다(벽 변경이 아니어도 오브젝트 스캔은 다시 돌아야 함 — 예: 제작소 방의 유일한 가공대를 철거하면 그 방이 "잡실"로 바뀌어야 함). 실제로 빈 상자/찬 상자, 배치 중/유휴 상태 제작대, 동물 있는/없는 목장, 작물 있는 밭 각각에 대해 철거 가능 여부가 규칙대로 동작하는지 확인한다.
- [x] #131 (완료 2026-09-05, QA 전체 스윕 — 결함 1건 발견, #132로 티켓 발행) [QA] (2026-09-05, BUILD/DESIGN 완료 5개 도달로 자동 추가 — loop/PROMPT_BUILD.md ③) 전체 스윕: 메인 메뉴부터 모든 시스템을 실제로 플레이해보며 문제를 찾는다.
- [x] #132 (완료 2026-09-05) [BUILD] (버그 리포트, #131 QA 전체 스윕에서 발견, 스크린샷으로 확인) `BUILD_GRID_SIZE`(#125가 64→16으로 축소한 값)에 건축물(벽/문/창문) 스프라이트 크기가 그대로 1:1로 묶여 있어서, 실제 게임 화면에서 벽/문이 **16×16px밖에 안 되는 작은 점처럼** 보인다. `world.gd:984`(`_spawn_structure()`의 `sprite.scale = Vector2(BUILD_GRID_SIZE, BUILD_GRID_SIZE) / tex_size`)와 `world.gd:860`(고스트 미리보기도 같은 공식)이 원인 — 캐릭터 키(약 102px, DESIGN.md "아이템/오브젝트 크기 표준" 기준점)와 비교하면 벽이 캐릭터 발 크기만도 못해서 거의 안 보이고, DESIGN.md가 정한 **최소 32px** 기준에도 못 미친다(벽/건축물은 "커도 되는" 카테고리로 명시돼 있을 뿐 이렇게 작아도 된다는 예외는 없음). 실제로 나무벽/나무문을 설치해서 스크린샷으로 확인(`game/qa/full_sweep.gd`의 `build_wall_ghost`/`build_wall_placed`/`build_door_ghost`/`deconstruct_wall_highlight` 스텝, `/tmp/qa67/40~44`, `50_deconstruct_wall_highlight.png` 참고 — 캐릭터 옆에 손톱만 한 갈색 사각형 하나만 겨우 보임)했고, `room_with_table` 스텝(45번 캡처)에서는 방을 이루는 벽 4개가 가공대 스프라이트(벽보다 훨씬 큼)에 완전히 가려져 화면에서 아예 안 보이는 것도 확인했다. **주의**: 이 문제는 새 그림이 필요한 게 아니라(기존 벽/문 텍스처는 정상) 격자(충돌/방 감지) 크기와 시각적 스프라이트 크기가 지금 하나의 상수로 묶여 있는 게 원인이므로 [BUILD]로 분류한다 — `_grid_occupancy`/방 감지(#122)가 쓰는 16 단위 격자 해상도는 그대로 유지한 채, 벽/문/창문 스프라이트의 **시각적** scale만 최소 32px 이상(가능하면 캐릭터와 비교해 자연스러운 비율)으로 키우는 방식을 검토할 것(예: 별도 시각 배율 상수를 도입하거나, 격자보다 조금 크게 그려지도록 스프라이트만 확대 — 충돌 `CollisionShape2D`의 `shape.size`는 건드리지 않아 방 감지/이동 충돌 판정에는 영향이 없어야 함). 고스트 미리보기(`_update_build_ghost()`)도 실제 설치물과 같은 시각 크기로 맞춰서 미리보기와 실제 결과가 다르게 보이지 않게 한다. 고친 뒤 벽/문을 설치해서 스크린샷으로 캐릭터 대비 비율이 자연스러운지 재확인한다.
- [x] #133 (완료 2026-09-05) [BUILD] (버그 리포트, 사용자 지시 2026-09-05, 스크린샷 첨부됨) 벽 격자 타일링을 딱 맞게 고친다. DESIGN.md "건축/방(Room) 시스템" 절의 2026-09-05 확정 내용("격자 타일과 벽 스프라이트는 반드시 1:1로 딱 맞아야 한다")을 먼저 읽는다. 지금 문제(사용자 확인): 벽을 겹치지 않게 최대한 붙여서 배치해도 화면상 빈틈이 보이고, 그 틈으로 플레이어가 지나갈 수 있으며, 방도 인식되지 않는다. 반대로 실제로 겹쳐서(또는 겹쳐 보이게) 설치해야 방이 만들어진다. 원인은 #132가 충돌 크기(`CollisionShape2D`, 그리드 해상도)는 그대로 두고 벽/문/창문의 **시각적 스프라이트만** 확대해서, 진짜 인접(비겹침) 배치가 오히려 겹쳐 보이는 착시를 만든 것이다. 이 항목에서는 #132 방식(스프라이트만 별도 확대)을 폐기하고, **`BUILD_GRID_SIZE`(`world.gd:101`) 자체를 충돌 크기 = 스프라이트 표시 크기가 정확히 1:1로 맞아떨어지면서도 시각적으로 자연스러운 값**(예: 24~32 사이, 실제로 스크린샷으로 비교해서 결정)으로 다시 조정한다. `_spawn_structure()`/`_update_build_ghost()`의 스프라이트 scale 계산과 `CollisionShape2D.shape.size`가 항상 같은 `BUILD_GRID_SIZE` 값 하나만 참조하도록 정리한다(별도 시각 배율 상수 제거). 방 감지(#122)의 flood-fill 탐색 상한(400칸)도 새 격자 크기 기준으로 다시 확인/조정한다. 실제로 벽 여러 개를 겹치지 않게 딱 붙여서 설치했을 때: 1) 화면에 빈틈이나 겹침 없이 이음매가 자연스럽게 이어져 보이는지, 2) 그 벽 라인을 플레이어가 통과할 수 없는지, 3) 벽 4개로 완전히 두른 공간이 방으로 인식되는지 스크린샷과 실제 플레이로 확인한다.
- [x] #134 (완료 2026-09-05) [DESIGN] (사용자 지시 2026-09-05, #133 이후 진행) 벽/문/창문이 실제 "서 있는 벽"처럼 보이도록 새로 그린다. DESIGN.md "건축/방(Room) 시스템" 절의 2026-09-05 확정 내용("벽은 서 있는 벽처럼 보여야 한다")을 먼저 읽는다. 지금은 기존 아이템 아이콘(평평한 나무판 그림)을 그대로 재사용해서 바닥 타일처럼 보인다는 지적(사용자, 스크린샷 확인 — 세로줄 무늬 갈색 사각형이 밭처럼 보임)이 있다. 나무벽/석제벽/강철벽/나무문/강철문/창문 각각을, 정면에서 봤을 때 두께/높이감이 있는 실제 벽처럼(코어키퍼/스타듀밸리 참고, DESIGN.md "그래픽 파이프라인" 규칙대로 절차적 생성 또는 SpriteCook 등 현재 쓰는 방식 활용) 다시 그린다. #133에서 확정된 `BUILD_GRID_SIZE`(충돌=표시 크기)에 정확히 맞는 치수로 만든다. 문은 기존처럼 열림/닫힘 상태 구분(반투명+오프셋 또는 새로 그린 열림 그림)을 유지한다. 벽을 여러 개 이어붙였을 때 이음매가 자연스러운지, 캐릭터와 비교해 비율이 어색하지 않은지 스크린샷으로 확인한다.
- [x] #135 (완료 2026-09-05) [BUILD] (사용자 지시 2026-09-05, #133 이후 진행 권장 — 정확한 벽 경계가 있어야 가림 판정이 정확함) 시야/전장의 안개(Fog of War) 시스템을 만든다. DESIGN.md "시야 / 전장의 안개(Fog of War, 코어키퍼 참고, 2026-09-05 확정)" 절을 먼저 전체 읽는다(방향/모양/가림/표현/범위/멀티플레이 규칙이 전부 그 절에 있다). 요약: 플레이어 위치를 꼭짓점으로, 기존 마우스 조준 방향을 중심으로 한 부채꼴(콘) 안에서만 보이게 하고, 콘 안이라도 벽/닫힌 문에 가로막힌 지점은 안 보이게 한다(직선 가로막힘 판정). 안 보이는 영역은 완전한 검은색(불투명)으로 덮는다 — #126의 반투명 방 오버레이(토글식)와는 별개의, 항상 켜져 있는 렌더링 레이어다. 콘 각도/반경은 DESIGN.md 지침대로 임의로 정하고 실제 플레이로 조정한다. 이동 충돌/방 감지/총알 물리는 건드리지 않는다(렌더링만). 멀티플레이에서는 각 클라이언트가 자기 플레이어 기준으로만 계산한다(다른 플레이어 화면에 영향 주지 않음). 실제로 벽으로 작은 방을 짓고 그 안팎을 오가며, 그리고 고개(마우스 방향)를 돌려가며 시야가 그에 맞게 바뀌고 벽 뒤가 안 보이는지 스크린샷으로 확인한다. 멀티플레이 환경(호스트+참가자 2개 클라이언트)에서 서로의 안개가 독립적으로 계산되는지도 확인한다.
- [x] #136 (완료 2026-09-05) [BUILD] (버그 리포트/설계 정정, 사용자 지시 2026-09-05, 스크린샷 비교 첨부됨 — 덕코프 vs 우리 게임) #135가 만든 시야 안개(`game/scripts/fog_of_war.gd`)를 덕코프 스타일로 순화한다. DESIGN.md "시야 / 전장의 안개" 절의 2026-09-05 수정/추가 내용을 먼저 읽는다(표현 변경, 벽 예외, 동물 처리 3가지 모두 그 절에 있다). 실제로 플레이해보니 지금 완전 불투명 검은색 안개가 덕코프(참고 스크린샷 — 시야 밖도 살짝만 어둡고 답답하지 않음) 대비 너무 답답하다는 지적. 세 가지를 고친다: 1) **`fog_of_war.gd:19`의 `FOG_COLOR := Color(0.0,0.0,0.0,1.0)`을 거의 티 안 나는 수준의 낮은 알파(예: 0.15~0.3 사이, 실제 스크린샷으로 비교하며 조정)로 낮춰서, 안 보이는 영역도 아주 살짝만 어둡게 보이고 바닥/지형은 원래 색이 은은하게 비치게 한다**(완전히 가리지 않음). 2) **벽/문/창문(`_grid_occupancy`에 있는 구조물)이 놓인 칸은 이 안개 예외로 처리해서 시야 콘/가림 판정과 무관하게 항상 정상적으로 보이게 한다** — `world.gd`에 벽/문/창문 존재 여부를 물어볼 수 있는 공개 접근자(예: `has_structure_at(cell)`)를 새로 추가해서 `fog_of_war.gd`가 내부 `_grid_occupancy`를 직접 건드리지 않고(기존 `is_cell_sight_blocked()`와 같은 패턴) 그 접근자로 확인 후, 구조물이 있는 칸은 `_draw()`에서 안개 사각형을 아예 그리지 않는다. 3) **동물(사슴 등)은 반대로 더 엄격하게 처리** — 안개가 옅어져도 동물만큼은 시야 콘 밖/가림 상태면 완전히 숨긴다(스프라이트 `visible = false`, 살짝 어둡게가 아니라 아예 안 보이게). `fog_of_war.gd`에 다른 스크립트가 "이 월드 좌표가 지금 보이는가"를 물어볼 수 있는 공개 함수(예: `is_position_visible(world_pos: Vector2) -> bool`)를 추가하고, 사슴(`game/scenes/deer/deer.gd`)이 매 프레임(또는 적당한 주기로) 자기 위치를 그 함수로 확인해서 안 보이면 숨긴다. 숨겨진 사슴이 이동 중(배회/도주 등 속도가 0이 아닌 상태)이면, 화면에 "이 방향에 뭔가 있다"는 걸 알리는 간단한 표시(느낌표나 소리 파형 아이콘 — 새 그림이 필요하면 기존 아이콘을 재사용하거나 간단한 도형으로 임시 처리해도 됨, 정교한 그림은 이번 범위 밖)를 잠깐 띄운다 — 정확한 위치가 아니라 대략적인 방향 힌트 수준이면 충분하다(예: 화면 가장자리 중 그 방향에 가까운 지점에 표시). 실제로: 안개가 덕코프처럼 옅게 보이는지, 벽으로 방을 지으면 그 벽 자체는 시야 밖에서도 계속 보이는지, 사슴이 시야 밖으로 나가면 완전히 사라지고 도주 중일 때 방향 힌트 아이콘이 뜨는지 스크린샷으로 확인한다.
- [ ] #137 [QA] 전체 스윕: 메인 메뉴부터 모든 시스템을 실제로 플레이해보며 문제를 찾는다. (BUILD/DESIGN 완료 카운터가 #132~#136(5개)로 5가 되어 자동 등록됨 — `loop/PROMPT_BUILD.md`③ 상시 규칙.)

## 취소된 항목 (참고용, 처리 안 함)

- ~~#76~#78 [DESIGN/BUILD] 캐릭터 기본 그림체 리메이크(성별 2종 전환)~~ — 2026-09-04
  사용자 지시로 큐에서 제거함. 근거: SpriteCook 유료 구독을 두 번 결제했는데도 바퀴
  130~131 동안 계속 잔액 0으로 막혀 있었고, 그 이전에도 비슷한 그림만 여러 번 뽑아내는
  비효율이 반복됐다 — 사용자가 "이 정도 속도면 에셋을 사는 게 낫다"고 판단, 캐릭터
  디자인은 직접 작업하거나 다른 방법을 다시 알아본 뒤 처리하기로 하고 하네스 큐에서는
  당분간 뺐다. **DESIGN.md "캐릭터 스타일 목표"/"성별 2종 전환" 결정 자체는 그대로
  유효하다** — 나중에 재개할 때 그 문서를 기준으로 다시 INBOX 항목을 추가하면 된다.

## 완료

-
